# Agent 内核成熟度评估：距「合格的 Agent」还有多远

> 评估时间：2026-09-20 ｜ 评估对象：`researchpilot/`（Sprint 1–3 已完成）+ `Sprint4_实施计划.md`
> 方法：**代码级取证**（不采信文档自述），参照 2026 年外部 Agent 能力基准的判据面。
> 结论一句话：**这是一个做得很扎实的 workflow 平台，Agent 内核为零。**

---

## 0. 三个数字

| 面 | 现况 | Sprint 4 后 | 说明 |
|---|:--:|:--:|---|
| **底座**（接入 / 治理 / 异步 / 可观测 / 降级 / 成本） | **≈85%** | ≈88% | 真正的资产，202 个测试在守 |
| **Agent 能力**（决策 / 工具 / 权限 / 记忆 / 评测） | **≈8%** | **≈55%** | Sprint 4 的全部战场 |
| **科研 Agent 能力**（技能层 + S1–S8 闭环 + 金标） | **≈12%** | ≈12% | Sprint 4 **完全不碰**，属 Sprint 5–11 |

差距不在「写得差」，而在**「手」还没长出来**，以及**长出来之后拿什么证明它合格**。

---

## 1. 判据：什么算「合格的 Agent」

### 1.1 定义判据（最关键的一条）

> 「下一步是否由**模型根据观察到的结果**决定。如果下一步是硬编码的，你手里的是 workflow，
> 不是 agent。」
> —— 2026 年业界对 agent / workflow 的分界共识

按这条判据，**今天的 ResearchPilot 是 workflow**：`Orchestrator.run_pipeline` 顺序执行
`STAGE_ORDER`，分支与回退边都还没接（设计 §6.3 的回退边在 Sprint 4 计划里也**不在内核**）。
这不是缺陷，是路线安排的必然结果——但也说明「距合格 Agent」的尺子应该量在这里。

### 1.2 基准面（2026 年实际在用的六类）

| 基准 | 测什么 | 对我们的意义 |
|---|---|---|
| **BFCL v4** | 工具调用准确率：并行调用、多跳、跨轮记忆、26 种 schema 表述变体 | 内核循环的**原语质量**，最贴近 US-404/405 |
| **τ²-bench** | 多轮对话中工具改变共享状态 + 必须遵守领域政策 | **与科研流程同构**：黑板就是共享状态，政策就是领域包 |
| **SWE-bench Verified** | 读现有代码库 → 改 → 过隐藏测试 | 「文件工具 + 真实任务」的成熟标尺 |
| **GAIA** | 跨工具链式任务，可到 50 步 | 长程任务不迷失的标尺 |
| **OSWorld** | 真实桌面操作 | 本系统不追求（无 GUI 自动化目标） |
| **Aider polyglot / MLE-Bench** | 编辑-测试循环 / 端到端 ML 工程自主性 | 与 S5 执行分支精神一致 |

一个反直觉但重要的事实：**前沿模型在孤立工具调用上已 >90%，在开放环境长程任务上仍多在
35–50%**。所以「能调工具」和「能自主完成研究」之间隔着整整一个数量级的工程工作量。

### 1.3 没有任何基准覆盖好的七个维度（自建系统必须自测）

这份清单直接决定了评估该看哪些地方，也正是本系统最容易「自我感觉良好」的地方：

1. **上下文压缩存活**——窗口满了压缩后，还记不记得「为什么否决了方案 B」
2. **跨会话持久**——上一轮的结论这一轮还拿得到吗
3. **行为一致性**——长会话中身份/风格/偏好是否漂移
4. **权限边界尊重**——会不会越权、会不会不问就删
5. **优雅降级**——工具挂了是降级还是整任务崩
6. **真实工具链**——有依赖、有副作用、输出可能是垃圾
7. **部署简单性**——用户装不装得上

第 1、2 条恰好命中设计 §5.2 Curator 的立项动机（「必须记下为什么放弃方案 B，
否则 Agent 反复走回死路」），而 **Sprint 4 不含 Curator**。

---

## 2. 现状实测（代码级）

### 2.1 后端：六条硬事实

| # | 事实 | 证据 |
|:--:|---|---|
| 1 | **唯一的 LLM 出口 `ChatRequest` 里没有 `tools`**。字段只有 `messages / tier / schema / max_tokens / temperature / model` | `app/ai/base.py:15-23` |
| 2 | **`ChatResult` 里没有 `tool_calls`**。只有 `text / provider / model / tokens / latency / degraded / attempts` | `app/ai/base.py:28-36` |
| 3 | **`capabilities` 声明了 `tools`，但零消费点**。`known_capabilities` 含 `tools`，`openai_compat` 默认带 `tools`——可 `grep` 全仓 `tool_choice\|function_call\|tool_calls` 只命中三处**声明**，没有任何一处**使用** | `base.py:108`、`providers/openai_compat.py:50`、`provider_config.py:25` |
| 4 | **`agents.tools` 是一列死数据**。表里有、DAO 收参数，但**没有任何读取点**。设计 §5.3 的 `AgentSpec.tools`（最小权限）从未落地 | `store/models.py:98`、`dao/agents.py:35` |
| 5 | **没有任何循环体**。`grep max_steps\|plan_execute\|KernelLoop\|tool_registry` → **0 命中** | 全仓 |
| 6 | **事件类型里没有工具语义**。只有 `job.*`（5 种）、`stage.*`（4 种）、`llm.start`、`llm.call` | `app/jobs/events.py:10-23` |

**这不是「差一点」，是缺一整层。** 换句话说：即使模型原生支持 function calling，
今天的网关**在协议层就无法表达一次工具调用**——这是 Sprint 4 第 3 步开工前的硬前置。

### 2.2 后端：已经很好的部分（不要重做）

| 能力 | 现状 | 对内核的价值 |
|---|---|---|
| 人机协同 | `approvals` DAO（create/get/list_by_status/decide）+ `/approvals/{id}/approve\|reject`；`job.paused` 语义 | **D4「复用而非另建」的判断是对的**，这是全项目最成熟的一块 |
| 预算治理 | `budget_grants` DAO + `BudgetExceeded → paused + 审批单`；预算批准后真能恢复（G1-3 已留证） | 内核熔断直接接线即可 |
| 中断/恢复 | `stage_checkpoints` DAO + `GET /projects/{id}/checkpoints`；`_skip_remaining` 暂停语义（FIX-07） | 步级内核检查点是它的自然延伸 |
| 流式可见 | `job_events.seq` = SSE `id`；`Last-Event-ID` 续传；`llm.start` **发起前**回调带候选链 | 「长等待可辨」的原语已解决，内核只加 `tool.call` |
| 降级容错 | 熔断 + 降级链 + 失败记账（`llm_usage.status/error/attempts`） | 第 5 条外部维度已达标 |
| 错误码体系 | §11.3 预留了 `EXEC-SANDBOX-001`、`AGENT-LOOP-003` 等域 | 命名先于实现，接线时不用重新发明 |

### 2.3 前端：四条硬事实

| # | 事实 | 证据 |
|:--:|---|---|
| 1 | **没有会话/消息概念**。`SessionPage` 的 7 个状态是 `project / stages / runs / blackboard / approvals / live / sysEvents`，没有 `messages`、没有 `conversation` | `pages/SessionPage.tsx:29-41`（299 行） |
| 2 | **输入框长得像聊天，语义还是命令**。`Command = {stage} \| {goal} \| {unknown}`，自由文本走 `goal` 分支——注释写「入口是说清楚你想研究什么」，但下游是**直接触发 S1**，不经过任何模型决策 | `components/CommandBar.tsx:5-8`、`48` |
| 3 | **`tool` 这个词已被占用，且含义不同**。`RunBlock.KIND_LABEL` 里 `tool`/`result` 指**Agent 自己写出来的步骤文本**，不是真实工具调用 | `components/RunBlock.tsx:5-10` |
| 4 | **轨迹渲染器是强资产**。thought/tool/result/decision/llm_call/**llm_pending** 六类步骤 + 内联黑板写入折叠 + `▶ replay` + 单块成本 + 已用时 | `RunBlock.tsx:92-246` |

第 3 条是**开工前必须定名的冲突**：Sprint 4 计划要新增事件 `tool.call` / `tool.result`，
与既有的 `tool` 步骤种类同名不同义，混进来会让「这一步是模型写的还是系统跑的」
在界面上不可分辨。

第 4 条是好消息：Sprint 4 第 7 步要做的「工具调用卡片」在这个渲染器上只是**增量**，
不是新建。

---

## 3. 差距矩阵（12 维）

打分：`0` 无 ｜ `1` 有雏形 ｜ `2` 最小可用 ｜ `3` 已加固。**合格线 = 2**。

| # | 维度 | 现在 | S4 后 | 关键缺口 |
|:--:|---|:--:|:--:|---|
| 1 | 决策式控制流 | 0 | 2 | `run_pipeline` 顺序执行；回退边不在 Sprint 4 |
| 2 | 工具调用原语 | 0 | 2 | **`ChatRequest` 无 `tools`**（P0） |
| 3 | 权限分级与沙箱 | 0 | 2 | D6 明说只做可验证最小面；完整沙箱在 S9 |
| 4 | 人机协同（审批/检查点） | 2 | 3 | — |
| 5 | 中断与检查点恢复 | 3 | 3 | `stage_checkpoints` 已在；内核步级检查点是延伸 |
| 6 | 跨会话记忆 | 0 | 1 | `conversations/messages` 建表 ≠ 记忆；**Curator 不在 Sprint 4** |
| 7 | 流式可见与轨迹回放 | 3 | 3 | — |
| 8 | 成本与预算治理 | 3 | 3 | — |
| 9 | 降级与容错 | 3 | 3 | 内核循环下的降级未单独验证 |
| 10 | 真实工具链韧性 | 0 | 1 | 有 D9 自愈、D10 截断；**工具间依赖 / 副作用回滚 / 脏输出**无对策 |
| 11 | 可评测性 | 1 | 1 | §12.5 的 golden / schema-test / judge 校准**在内核层无对应项** |
| 12 | 部署简单性 | 0 | 0 | Sprint 11 才交付，已知延后 |

**读法**：第 4、5、7、8、9 行是绿的——这些是很多人做到一半才补的东西，这里已经有了。
**红的是第 1、2、3、6、10、11 行**，其中第 2、11 行是 Sprint 4 计划**当前没写清楚**的。

---

## 4. Sprint 4 计划覆盖度评审

### 4.1 判断正确的决策（无需改）

- **D1 内核是手、编排层是总指挥**：避免两套「下一步做什么」的真相。方向对。
- **D2 对话层不持研究状态**：与设计 §352 一致，也躲开了「聊天记录当事实来源」这个坑。
- **D3 循环跑在现有 `JobRunner` worker 里**：SSE 续传/取消/自愈已经验证过一遍，复用是正解。
- **D4 复用 `approvals` + `job.paused`**：全项目最成熟的资产，另建会让用户看到两种审批卡。
- **D5 权限等级是静态契约、不由模型决定**：这条是安全底线，措辞也对——「让模型自称只读等于没有分级」。
- **D6 写清「不做什么」**：比含糊声称「有沙箱」负责得多。
- **D9 按 `(tool, args_hash)` 计自愈次数**：正确的细节——按工具名计会误杀正常的换参重试。
- **D10 超限结果头尾保留 + 截断提示**：与 FIX-06 同一原则，不静默撒谎。

### 4.2 计划里的三个真实缺口

**缺口 ①（P0，阻塞性问题）：`tools` 协议层没打通**

Sprint 4 第 3–4 步要建注册表和调用循环，但**第 3 步之前必须先把 LLM 出口改造成能承载工具调用**：

- `ChatRequest` 增 `tools: list[dict] | None` 与 `tool_choice`
- `ChatResult` 增 `tool_calls`
- `openai_compat` 双向透传（请求体与响应解析）
- ⚠️ **`degrade._request_with` 手工重建 `ChatRequest`**——不同步它，重试时工具定义会被静默丢掉
  （`model` 字段已经踩过这个坑，见项目记忆）
- mock provider 要能产出**确定性的工具调用**，否则内核测试无法离线跑

这一项在 `Sprint4_实施计划.md` 里**完全没有出现**。不补它，第 4 步的循环无从下手。

**缺口 ②（P0，命名冲突）：`tool.*` 事件与既有 `tool` 步骤种类撞名**

`RunBlock.KIND_LABEL` 的 `tool` 是「Agent 写的步骤」，新事件 `tool.call` 是「系统真的执行了工具」。
必须在第 3 步就定名（建议：事件统一 `tool.call` / `tool.result`，步骤种类保留 `step.tool`
或把旧的改名为 `note.tool`），否则前端接线时「模型声称做了」和「系统真的做了」会混在同一张卡上——
而这个区分恰恰是审计价值的全部来源。

**缺口 ③（P1，文档引用错位）：`工具契约（对齐设计 §3.3）`**

设计 §3.3 实际是**领域包的 `pack.yaml` 接口**，不是工具契约。工具契约在 **§5.3 `AgentSpec`**
与 **§10.1 工具权限分级**。`Sprint4_实施计划.md:113` 的引用需修正——这类错引会让后来者
在错误的章节里找接口定义，而 §5.3 里 `AgentSpec` 的 `reads/writes/max_steps/max_cost_usd/
requires_critic/human_checkpoint` 六个字段**至今一个都没实现**，是内核算法的真正输入。

### 4.3 计划内部的一处矛盾（需要拍板）

| 位置 | 说法 |
|---|---|
| `修订排期与修复计划.md:247`（G2 闸口） | 内核验收**包含「`deterministic` 模式可用」** |
| `修订排期与修复计划.md:266`（12 周裁剪方案） | 「S4 内核压缩为『工具注册表 + 调用循环 + 权限闸门』，**暂缓并行调用、上下文摘要、确定性模式**」 |

闸口要求的东西，裁剪方案第一个砍。**这两条不能同时成立。**
建议：要么保留 deterministic（它是 G2-7 的全部证据来源，且实现成本低——
固定 `plan_execute` + `temperature=0` + 固定 seed + 禁并行），要么**同步改写 G2**，
明确「取消第 7 条」。**不能留着一个自己达不到的闸口。**

### 4.4 计划整体够不够？

**够到「最小可用的手」**：工具注册表 + 调用循环 + 权限闸门 + 步级检查点，
做完这些 G2 的 7 条能过。

**不够到「合格的 Agent」**，缺的是：

1. **评测**：G2 只有「跑通」没有「质量」。§12.5 要求确定性组件单测覆盖 ≥90%、
   schema 解析率 ≥98%、judge 校准 κ≥0.6——内核层面**一条都没有对应**。
   至少应把 G2-7（两次跑出同样的步骤序列）升级为**可断言的 golden case**，而不是
   「开关存在」。
2. **压缩存活探针**：D8 给了裁剪优先级，但没有任何测试验证「压缩后还记得为什么否决方案 B」。
   这正是 Curator 的立项动机，也是外部清单第 1 条。
3. **真实工具链**：D9/D10 只覆盖了「重试」和「截断」。工具 A 的输出是工具 B 的输入、
   写操作失败如何回滚、工具返回看似合法实为垃圾——这三类今天**没有对策**，
   而它们恰恰是「benchmarks 不测但生产必炸」的部分。
4. **Curator 跨会话记忆**：Sprint 4 完全没有。这会让外部维度第 2 条长期挂空。

---

## 5. 建议的动作顺序

### 开工前（必须先做，都是小改动）

- [ ] **补 P0 缺口 ①**：`ChatRequest.tools` / `ChatResult.tool_calls` / `openai_compat` 透传 /
      `degrade` 同步 / mock 产出确定性工具调用。**归入 Sprint 4 第 3 步之前**。
- [ ] **定名缺口 ②**：`tool.call` / `tool.result` 与既有 `tool` 步骤种类的关系写进计划。
- [ ] **修正缺口 ③**：`Sprint4_实施计划.md` 里 §3.3 → §5.3 / §10.1。
- [ ] **拍板 4.3 的矛盾**：deterministic 保留 or 改写 G2。
- [ ] **G1-1 闭环**：用户录入真实 OpenRouter Key（当前是 8 字符占位），复跑
      `tmp/smoke_g1_real_s1.py`。内核验收不依赖它，但 G2 要求「真实任务」。

### Sprint 4 内（建议加，成本低价值高）

- [ ] 把 G2-7 升级为 golden case（`temperature=0` + 固定 seed → 步骤序列逐项相等），
      同时它就是 §12.5 在内核层的第一个样本。
- [ ] 加一条「压缩存活」测试：裁剪发生后仍能答出上一步的否决理由。
- [ ] 让 `AgentSpec.tools`（或现有 `agents.tools` 列）真正收口工具白名单——
      否则 §5.3「权限最小化」只是文档里的一句话。
- [ ] 内核循环跑在真实降级链下的用例（工具超时 / provider 熔断 → 任务是否优雅降级）。

### 明确延后（要写「延后」，不要变成遗漏）

- Curator 跨会话记忆 → 建议提前到 Sprint 5 开头，它对「像不像一个连续工作的助手」影响很大
- 回退边与 Critic 否决 → S8（已排）
- 完整沙箱（受限令牌 / Job Object）→ S9（已排）
- 部署简单性 → S11（已排）

---

## 6. 结论

**Sprint 1–3 把地基做完了，而且做得比大多数同类项目好**：真实模型接入可换、失败也算一次调用、
长等待在界面上可辨、审批暂停恢复有留证、迁移安全逐表核对过。这四层是**别人做到一半才回头补**的东西。

**但「Agent」这三个字所指向的那一层，目前是空的**——不是「不够好」，是**一行都没有**：
没有工具、没有循环、没有计划对象、没有权限判定。`ChatRequest` 里连 `tools` 这个字段都不存在。

**Sprint 4 是一份合格的「从 0 到最小可用」计划**，8 步顺序对、决策理由对、
风险与裁剪写得诚实（尤其 D6 明说不做完整沙箱，这种克制比夸大更可信）。

**要真正称得上「合格的 Agent」，还差三件事，其中两件 Sprint 4 应该顺手做掉**：

1. **把协议层打通**（`tools` 端到端）——否则第 4 步开工即卡住。**不做不行。**
2. **把「跑通」升级为「可断言」**——deterministic 从开关变成 golden case，
   否则 G2 通过之后你仍然不知道它在质量维度上处于什么位置。
3. **承认记忆与评测是 Sprint 5 的债**——写下它，而不是等到阶段二才发现
   「为什么它不记得上一轮否决过什么」。

距离用一句话概括：**平台成熟度约 85%，Agent 成熟度约 8%；Sprint 4 把 Agent 成熟度推到约 55%，
而「合格的科研 Agent」这条线在 Sprint 9–11 之后。**
