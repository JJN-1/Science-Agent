"""Agent 内核（Sprint 4，阶段一）。

分层职责见设计 §4.1：内核是**手**（工具注册表 + 调用循环 + 权限闸门 + 上下文装配），
编排层仍是**总指挥**（阶段调度、回退边、检查点）。两者不互相复制「下一步做什么」的真相。

当前已落地：``tokens``（估算）、``context``（装配与裁剪）、``errors``（错误码）。
后续步骤见 ``Sprint4_实施计划.md``。
"""
