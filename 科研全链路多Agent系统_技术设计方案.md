# 科研全链路多 Agent 系统 · 技术设计方案

**版本**：v1.0　**日期**：2026-09-18　**许可**：Apache-2.0
**系统代号**：ResearchPilot
**交付形态**：开发期为本地 Web 应用，交付期为桌面安装包
**主攻学科**：计算机 / 人工智能（内置领域包，其他学科通过领域包扩展）

---

## 1. 项目定位

### 1.1 一句话定义

一个**覆盖科研全流程的本地多 Agent 协作系统**：用户给出一个研究方向，系统自主完成"发现空白点 → 文献综述 → 形成可证伪假设 → 设计实验 → **能跑的真的跑给你看** → 分析结果 → 生成带证据锚点的草稿 → 打包复现物"，每一步都留下可追溯的证据与可复现的路径。

### 1.2 它与"文献工具"的本质区别

| 维度 | 一般文献 AI 工具 | 本系统 |
|---|---|---|
| 覆盖范围 | 文献问答（全链路的 1/8） | 科研全流程 8 个阶段 |
| 智能体形态 | 固定工作流（workflow） | **目标驱动的多 Agent 编排**，含动态规划与回退边 |
| 权限 | 只读文献 | **读写科研资产**（跑代码、写数据、写草稿） |
| 执行能力 | 无 | **条件执行**：计算机类研究真实跑实验，非计算类交付可执行方案 |
| 可信度 | 引用可选 | 引用可溯源 + **计算可复现** + 决策可解释 |
| 数字来源 | 模型生成 | **全部由确定性引擎计算，模型不碰数字** |

### 1.3 做 / 不做

| 做 | 不做 |
|---|---|
| 全链路 8 阶段 Agent 编排 | 自动投递投稿、代替作者署名 |
| 文献检索聚合、解析、精读、带溯源问答 | 版权文献自动下载与二次分发 |
| 研究问题发现、可证伪假设形式化 | CNKI / IEEE Xplore 违规接入 |
| 实验设计与样本量估算（确定性计算） | 接入真实实验室仪器与临床系统 |
| **计算机类实验的真实执行**（代码→沙箱→结果） | 自动撰写完整论文（只做带证据锚点的结构化草稿） |
| 统计分析与图表（确定性引擎） | 生成未经检验的"结论" |
| 复现包打包与可跑通校验 | 绕过伦理审查的任何动作 |
| 领域包扩展（其他学科） | 硬件依赖（传感器、边缘设备、PLC） |

### 1.4 核心指标

| 指标 | 定义 | 目标 |
|---|---|---|
| 引用可溯源率 | 有可校验引用的论断占比 | ≥ 95% |
| 证据充分率 | 引用片段确实支撑该论断的比例 | ≥ 90% |
| **计算可复现率** | 所有数字由确定性代码产出，同一 `analysis_run` 重跑结果一致 | **100%** |
| **决策可解释率** | 每个研究决策都有证据链与理由记录 | ≥ 95% |
| **执行真实性标注率** | S5 产出必须显式标注"真实执行"或"方案交付" | **100%** |
| 结构化抽取成功率（含重试） | Schema 校验通过率 | ≥ 98% |

### 1.5 学科策略：内置计算机类 + 领域包扩展

**主攻计算机 / 人工智能**，理由：

1. arXiv CS 数据最全，检索与解析质量最高
2. **S5 大部分研究天然可接入**（算法实验、模型训练、仿真、性能测试），能真正跑出全链路闭环
3. 时间切片金标最容易构造（arXiv 有完整的版本历史与时间线）
4. 评测指标客观（准确率、F1、BLEU、延迟、收敛性）

**同时保留其他学科能力**：所有学科相关内容（术语表、领域规则、报告规范、S5 判定规则、金标来源）都抽到**领域包（Domain Pack）**中，核心架构与学科无关。详见 §3。

---

## 2. 科研全链路 8 阶段

| 阶段 | 名称 | 核心问题 | Agent | 深度分级 |
|:---:|---|---|---|---|
| **S1** | 选题发现 | 这个领域还有什么没被解决？ | Scout | 原型级（LLM 主导 + 人工选择） |
| **S2** | 文献综述 | 已有工作做到了哪一步？ | Librarian | **生产级**（确定性引擎 + 可评测） |
| **S3** | 假设形式化 | 我要验证的到底是什么？ | Formalizer | 原型级（LLM 主导 + 人工确认） |
| **S4** | 实验设计 | 怎么设计才能验证它？ | Designer | **生产级**（确定性引擎 + 可评测） |
| **S5** | 执行采集 | 把实验真正跑出来 | Executor | **条件执行**（见 §7.5） |
| **S6** | 分析解读 | 数据说明了什么？ | Analyst | **生产级**（确定性引擎 + 可评测） |
| **S7** | 写作成稿 | 怎么把它讲清楚？ | Writer | 原型级（LLM 主导 + 人工确认） |
| **S8** | 投稿复现 | 怎么让别人也能跑出来？ | Publisher | 原型级（含确定性打包与校验） |

**深度分层的理由**：8 个阶段各是专业领域，全部做到生产级不现实。把"有确定性组件、可客观评测"的阶段做深（S2 / S4 / S6），其余做到"可用 + 人工兜底"。

---

## 3. 领域包机制（Domain Pack）

### 3.1 设计目标

核心架构**完全学科无关**。所有学科相关内容通过领域包注入，使系统"主攻计算机类，但换一个包就能服务其他学科"。

关键约束：**领域包是纯声明式配置 + 可选脚本，不侵入核心代码**。

### 3.2 包结构

```
domain_packs/
├── cs-ai/                     # 内置，默认启用
│   ├── pack.yaml
│   ├── terminology.yaml        # 术语表（中英对照 + 别名）
│   ├── rules/
│   │   ├── statistical_tests.yaml
│   │   ├── reproducibility_checklist.yaml
│   │   └── report_standard.yaml
│   ├── venues.yaml             # 期刊/会议与格式要求
│   └── goldset.yaml            # 评测金标来源
└── <其他学科>/                 # 社区或用户自行添加
```

### 3.3 包接口（`pack.yaml`）

```yaml
id: cs-ai
name: 计算机 / 人工智能
version: 1.0.0
extends: null

# ── 术语与语言 ──────────────────────────────
terminology:
  file: terminology.yaml
  enforce_consistency: true          # S7 启用术语一致性检查

# ── 领域规则（确定性，不靠模型）──────────────
rules:
  statistical_tests: rules/statistical_tests.yaml      # S6 检验选择
  reproducibility:   rules/reproducibility_checklist.yaml  # S8 复现包校验
  report_standard:   rules/report_standard.yaml        # S7 结构规范

# ── 报告与投稿规范 ──────────────────────────
reporting:
  structures: [imrad]                # 论文结构模板
  venues: venues.yaml                # 期刊会议注册表
  reference_style: ieee              # 默认引用格式（CSL）

# ── S5 可接入性画像（决定能不能真跑）────────
execution_profile:
  default_research_type: computational
  auto_evaluable_metrics:
    - accuracy
    - top1_accuracy
    - f1
    - precision
    - recall
    - auc
    - mae
    - rmse
    - perplexity
    - bleu
    - rouge
    - latency_ms
    - throughput
    - convergence_epoch
  sandbox_preset: gpu-lite            # 沙箱资源模板
  typical_artifact_kinds: [code, dataset_ref, checkpoint, log, figure]

# ── 评测金标 ────────────────────────────────
goldset:
  source: arxiv-cs
  method: time_slice                  # 时间切片法
  cutoff_year: 2023

# ── 图谱节点类型（S1 空白点扫描用）──────────
graph:
  node_types: [Paper, Author, Institution, Method, Dataset, Metric, Task, Limitation]
  gap_patterns:                       # 空白点图模式，可被 Scout 查询
    - "Method M 未在 Dataset D 上评估过"
    - "Task T 上 Method 族 F 未被应用"
    - "Limitation L 被多次提及但无人解决"
```

### 3.4 领域包如何影响各阶段

| 阶段 | 领域包提供什么 | 不提供时的默认行为 |
|:---:|---|---|
| S1 | 图谱节点类型 + 空白点图模式 | 用通用引用/作者/机构图谱 |
| S2 | 术语表（用于精读卡片归一化） | 不做术语归一 |
| S4 | 指标注册表、典型实验范式 | 通用设计检查 |
| **S5** | **可自动评价指标清单 + 沙箱模板** | **保守判定为不可接入 → 方案交付** |
| S6 | 统计检验选择规则 | 内置通用规则（参数/非参数基础集） |
| S7 | 结构规范 + 引用格式 + 术语一致性 | 通用 IMRaD + GB/T 引用 |
| S8 | 复现性清单 + 期刊注册表 | 通用复现包清单 |

> **S5 的默认行为是保守的**：没有领域包声明"什么指标可自动评价"时，系统判定为不可接入，走方案交付分支。这保证了"不会假装跑成功"。

### 3.5 扩展示例：生物医学包（证明可扩展性）

```yaml
id: biomed
name: 生物医学
version: 1.0.0

rules:
  statistical_tests: rules/statistical_tests.yaml   # 增加生存分析、混合效应模型
  reproducibility:   rules/arrive_checklist.yaml    # ARRIVE 动物实验报告规范
  report_standard:   rules/prisma_2020.yaml         # PRISMA 2020 系统综述清单

reporting:
  structures: [imrad, prisma]
  reference_style: vancouver

execution_profile:
  default_research_type: wet_lab        # ← 默认判定不可接入
  auto_evaluable_metrics: []            # ← 无自动指标，保守走方案交付
  sandbox_preset: none

goldset:
  source: pubmed-oa
  method: time_slice
  cutoff_year: 2023
```

**可以看到**：换一个包，S5 自动从"真实执行"切到"方案交付"，S6 的检验规则换成医学统计，S7 的报告规范换成 PRISMA/ARRIVE——**核心代码一行不改**。

**特例提醒**：生物医学下的**元分析**仍可走真实执行分支（效应量合并是确定性统计），这由 S5 的研究类型判定单独识别，不受 `default_research_type` 限制。

---

## 4. 系统架构

### 4.1 分层

```
界面层    React 19 + TS + Vite + Ant Design 6 ｜ AntV G6（图谱）｜ SSE 进度
          会话流 · 计划卡片 · Agent 轨迹回放 · 人工检查点 · 报告视图
──────────────────────────────────────────────────────────────────────
服务层    FastAPI（单进程，127.0.0.1，启动令牌校验）
          ├ 同步端点  检索 / 查询 / CRUD / 设置
          └ 异步端点  阶段运行 / 管道运行 / 执行实验 / 导出   立即返回 jobId
──────────────────────────────────────────────────────────────────────
内核层    Agent Kernel（阶段一）
          任务拆解 · 工具注册表与调用循环 · 权限闸门 · 上下文装配 · 检查点恢复
          对话式入口；S1–S8 确定性编排作为其一种技能
──────────────────────────────────────────────────────────────────────
编排层    Orchestrator（Supervisor 模式）
          阶段调度 · 动态路径 · 回退边 · 检查点 · 人工中断点
          ├ 阶段 Agent × 8
          └ 支撑 Agent × 4（Critic / Curator / Steward / Human 接口）
──────────────────────────────────────────────────────────────────────
任务层    进程内 asyncio JobWorker
          jobs 表持久化 · 指数退避 · 取消 · 断点续跑 · 沙箱子进程调度
──────────────────────────────────────────────────────────────────────
AI 能力层 ①检索聚合 ②解析路由 ③向量检索 ④Agent 推理 ⑤图谱查询 ⑥统计计算
          统一出口 ProviderRegistry（§8）
──────────────────────────────────────────────────────────────────────
执行层    ★ 代码执行沙箱（目录白名单 · 默认禁网 · 资源配额 · 产物哈希）
          统计计算引擎 · 绘图 · LaTeX 编译 · CSL 引用格式化 · Git
──────────────────────────────────────────────────────────────────────
数据层    SQLite（WAL）：业务数据 / FTS5 全文索引 / 向量 BLOB / 图谱关系
          文件系统：PDF 原件、LaTeX 源码、实验产物、复现包
──────────────────────────────────────────────────────────────────────
领域层    ★ Domain Pack（cs-ai 内置 / 可扩展）
──────────────────────────────────────────────────────────────────────
横切      安全（§10） · 可观测（§13） · 配置与密钥（§8.4）
```

### 4.2 运行拓扑

```
启动器 / uvicorn（127.0.0.1:<自动端口>）
   ├─ /api/*                业务接口
   ├─ /static/*             前端产物
   └─ /api/jobs/{id}/stream SSE 进度

Orchestrator（同进程 asyncio）
   ├─► 阶段 Agent ──► Blackboard（结构化共享状态，SQLite）
   ├─► 支撑 Agent ──► Curator（记忆）/ Critic（评审）/ Steward（治理）
   └─► ProviderRegistry ──► 用户自定义模型后端（§8.1）
                            ├ OpenAI 兼容端点
                            ├ Anthropic 兼容端点
                            └ 本地自建端点（如 Ollama，非交付必需）

JobWorker ──► 沙箱子进程（受限令牌 + 目录白名单 + 资源配额 + 禁网）
```

### 4.3 开发期与交付期形态

| 阶段 | 形态 | 理由 |
|---|---|---|
| 开发期（Sprint 1–9） | 本地 Web 应用：`uvicorn` + `vite dev` | **不让打包阻塞敏捷迭代** |
| 开发期（Sprint 10） | `vite build` + FastAPI 静态托管，单进程 | 提前暴露集成问题 |
| 交付期（Sprint 11） | 桌面安装包（pywebview + python-build-standalone + Inno Setup） | 用户不接触命令行 |

**打包策略沿用可靠方案**：捆绑独立 CPython 运行时，源码 + venv 交付，不用 PyInstaller（隐藏导入复杂、启动慢、易被误报）。体积目标 < 300 MB。

### 4.4 数据目录

```
%APPDATA%\ResearchPilot\
  ├─ app.db              SQLite 主库
  ├─ files\              PDF 原件
  ├─ tex\                arXiv LaTeX 源码
  ├─ workspace\          ★ 用户科研工作区（实验代码/数据/产物，可指向用户自己的 Git 仓库）
  ├─ packs\              ★ 领域包（内置 + 用户添加）
  ├─ logs\
  └─ config.yaml
```

备份 = 复制该目录。

---

## 5. Agent 编制

### 5.1 阶段 Agent（8 个）

| Agent | 阶段 | 职责 | 档位 | **关键确定性组件（自研，不靠模型）** |
|---|:---:|---|---|---|
| **Scout 侦察** | S1 | 扫描领域图谱找空白点，生成候选研究问题并做可行性初筛 | `plan` | 空白点图模式查询；热点增长率计算（近 2 年增长率 >50% 且规模 >20 篇） |
| **Librarian 文献** | S2 | 迭代检索（自判证据饱和）、解析、精读卡片、带引用问答 | `extract` / `summarize` | 去重算法；RRF 融合；引用坐标校验器；检索策略记录器 |
| **Formalizer 假设** | S3 | 候选问题 → 可证伪假设 + 操作化定义 + 变量设计 | `plan` | **可证伪性检查器**；变量完备性检查（自变量/因变量/控制/混淆） |
| **Designer 设计** | S4 | 实验方案、对照组、样本量估算、指标选择、消融设计 | `plan` / `synthesize` | **样本量估算器**（power analysis）；**设计模式检查器** |
| **Executor 执行** | S5 | 条件执行：可接入则真跑，不可接入则交付方案 | `plan` / `write` | **接入可行性判定器**；环境快照器；沙箱调度；失败分类器；结果登记器 |
| **Analyst 分析** | S6 | 统计检验、图表、结果与文献对比 | `synthesize` | **统计检验选择规则引擎**；效应量与置信区间；多重比较校正 |
| **Writer 写作** | S7 | 结构化草稿（IMRaD）、段落级引用、术语一致性 | `write` | **CSL 引用格式化**；术语一致性检查器；论断-证据锚点绑定器 |
| **Publisher 发表** | S8 | 期刊匹配、投稿清单、复现包打包、评审响应框架 | `synthesize` | 复现包校验器；投稿材料清单校验 |

### 5.2 支撑 Agent（4 个，横切全链路）

| Agent | 职责 | 关键设计 |
|---|---|---|
| **Critic 评审** | 独立校验每个阶段产物 | **必须用不同厂商的模型**（异构交叉验证）；**绝不能是产出方**；有否决权 |
| **Curator 记忆** | 项目记忆：研究日志、决策记录、失败实验、假设演化树 | **"为什么放弃方案 B"必须记下来**，否则 Agent 反复走回死路 |
| **Steward 治理** | 预算熔断、步数上限、工具权限审批、危险操作确认、伦理护栏 | 见 §10 |
| **Human 人类** | 不是 Agent，但是编排中的**一等节点** | 检查点、审批、假设选择、结论签署——科研决策不能全自动 |

### 5.3 Agent 契约

```python
class AgentSpec(BaseModel):
    id: str                              # scout | librarian | ...
    stage: str                           # S1..S8
    tier: Literal["extract", "plan", "critique", "synthesize", "write"]
    tools: list[str]                     # 允许调用的工具（最小权限）
    reads: list[str]                     # 可读黑板对象类型
    writes: list[str]                    # 可写黑板对象类型
    max_steps: int                       # 步数上限
    max_cost_usd: float                  # 成本上限
    requires_critic: bool                # 产物是否强制评审
    human_checkpoint: Literal["none", "before", "after", "risk_based"]
```

**权限最小化原则**：处理文献内容的 Agent 不持有文件写权限、不持有网络工具、不持有密钥。只有 Executor 持有沙箱写权限，只有 Steward 持有审批权。

---

## 6. 编排架构

### 6.1 候选拓扑对比

| 拓扑 | 结构 | 优点 | 缺点 | 采用 |
|---|---|---|---|---|
| **Supervisor 主管式** | 一个 Orchestrator 动态派活 | 灵活、可动态改路径、状态集中可见 | 主管上下文压力大 | **✅ 顶层** |
| **Pipeline 流水线** | 固定顺序 A→B→C | 简单 | **这就是 workflow，不是编排** | ❌ |
| **Blackboard 黑板式** | 所有 Agent 读写共享工作区 | 解耦、易扩展、无信息丢失 | 全局状态难控 | **✅ 作为共享状态层** |
| **Hierarchical 分层** | Orchestrator + 阶段 Lead + 子 Agent | 可扩展 | 复杂度高 | ⚠️ 仅 S2 精读用（Map-Reduce） |
| **Group Chat 群聊式** | 多 Agent 对话协商 | 适合辩论评审 | 成本高、易发散 | ✅ 仅用于对抗评审 |

### 6.2 选定：Supervisor + Blackboard + 对抗评审

三条理由：

1. **顶层必须 Supervisor** —— 科研阶段是**强依赖**的（假设决定设计，设计决定分析），纯黑板事件驱动会让路径失控
2. **共享状态必须用结构化 Blackboard** —— 存对象（研究问题、假设、变量表、实验方案、结果指标），**不存聊天记录**，否则多个 Agent 对同一概念的理解会漂移
3. **评审必须对抗式** —— 多 Agent 系统最危险的失效是**"互相吹捧"（sycophancy loop）**：A 产出、B 点赞、C 通过。Critic 必须独立、异构、有否决权

### 6.3 控制流（含回退边）

```
用户：研究目标（一句话）
   ↓
Orchestrator 建立 Blackboard，加载领域包，生成初始路线图
   ↓
╔═ 阶段循环 ════════════════════════════════════════════════╗
║                                                           ║
║   Scout ⇄ Librarian      空白点与文献互相触发，可多轮往复    ║
║        ↓                                                  ║
║   Formalizer →【对抗评审】→ 假设定稿     ★人工选择假设       ║
║        ↓                   ↑                              ║
║   Designer  →【设计审查】→ 实验方案      ★人工确认方案       ║
║        ↓                   ↑                              ║
║   Executor【接入可行性判定】                                ║
║      ├─ 可接入  → 真实执行（代码→沙箱→结果→自愈循环）        ║
║      └─ 不可接入 → 方案交付（协议/SOP/采集表/预登记分析计划） ║
║        ↓                   ↑                              ║
║   Analyst →【统计一致性校验】→ 分析结果                     ║
║        ↓                   ↑                              ║
║   Writer  →【引用真实性校验】→ 结构化草稿                   ║
║        ↓                   ↑                              ║
║   Publisher → 复现包 + 投稿清单                            ║
║                                                           ║
║   ★ 回退边：Critic 否决可退回上一阶段甚至更早阶段重做        ║
╚═══════════════════════════════════════════════════════════╝
   ↓
全程：Curator 写记忆 · Steward 管预算与权限 · 每阶段存检查点
```

**回退边的实际价值**：Analyst 发现"结果不显著"可退回 Designer 提议改样本量；Writer 发现有论断无证据可退回 Librarian 补检索。这是与固定流水线最本质的区别。

### 6.4 编排协议

- Agent 输出一律**结构化**（Pydantic Schema），不返回自然语言就完事
- 黑板对象**版本化**：`hypothesis@v3`、`experiment_plan@v2`
- Agent 间**不点对点传话**，一律经黑板（避免信息丢失与责任模糊）
- 每个产物标注**产出 Agent、依赖对象、证据锚点**
- 编排层持 `stage_checkpoints`，任一阶段可单独重跑
- 全程 `agent_runs` / `agent_steps` 轨迹，支持**回放**

### 6.5 中断点与人工检查点（按风险分级）

| 风险级 | 典型动作 | 处理 |
|---|---|---|
| 低 | 检索、解析、生成草稿、绘图 | 自动通过，事后可审 |
| 中 | 假设定稿、实验方案确定、分析结果解读 | **事后审**：产出后必须人工确认才进入下一阶段 |
| 高 | 选定研究问题、真实执行实验、写正式文件、删除/覆盖数据、Git push、对外发送 | **事前批**：必须人工批准，不可配置关闭 |

---

## 7. 各阶段详细设计

### 7.1 S1 Scout —— 选题发现

**输入**：研究方向（自由文本）、领域包
**输出**：候选研究问题清单 + 每个问题的空白点证据 + 可行性评分

**流程**
1. 以研究方向为种子检索近 5 年文献，构建子领域图谱
2. **空白点扫描**（基于领域包声明的图模式，确定性查询）：
   - "方法 M 未在数据集 D 上评估过" → 查 `APPLIES` / `EVALUATED_ON` 关系的缺失
   - "任务 T 上方法族 F 未被应用" → 查方法族与任务的笛卡尔积缺口
   - "局限 L 被多次提及但无人解决" → 统计 `LIMITATION` 节点入度
3. 热点识别：摘要嵌入 + HDBSCAN 聚类，3 年滑动窗口；热点判定 = 近 2 年增长率 >50% 且绝对规模 >20 篇
4. 候选问题生成：LLM 基于空白点证据生成，**每个问题必须绑定至少一条空白点证据**，无证据的不输出
5. 可行性评分：领域包是否可接入 + 数据可得性 + 计算代价 + 方法成熟度
6. ★ **人工选择**：用户从候选中选定 1 个（或修改后确认）

**确定性组件**：图谱缺失关系查询、热点增长率计算、可行性评分加权。

### 7.2 S2 Librarian —— 文献综述

**输入**：研究问题、领域包
**输出**：文献池 + 精读卡片 + 知识地图 + **可复现检索策略记录**

**流程**
1. 多源并行检索（OpenAlex / Crossref / arXiv / Semantic Scholar，每源独立令牌桶）
2. 字段归一化 → 去重（DOI 精确 → 标题+年份相似度 > 0.92 → 作者姓氏+年份）→ 排序（相关性 0.5 + 归一化引用量 0.3 + 时效性 0.2）
3. 解析管线：元数据嗅探 → 特征判定（文本层/公式/表格/双栏/扫描件）→ 路径路由（arXiv LaTeX > 本地 pdfplumber > 在线服务 > RapidOCR）→ 章节切分 → 质量评分 0–100
4. 分块（512 token / 15% 重叠）+ 向量化 + FTS5 → 混合检索（dense + sparse + lexical → RRF → rerank）
5. **迭代检索**：Agent 判断证据是否饱和（新增文献不再带来新概念/新方法/新数据集），未饱和则改写检索式补漏
6. 逐篇精读卡片（并发 Map，Schema 强约束，失败带错误信息重试）
7. ★ **人工确认文献池**（中断点）

**确定性组件**：去重算法、RRF 融合、引用坐标校验、**检索策略记录器**（记录检索式、时间、结果数，使检索可复现——这是系统综述的硬要求）。

### 7.3 S3 Formalizer —— 假设形式化

**输入**：选定的研究问题、文献证据
**输出**：假设 + 操作化定义 + 变量表 + **否证条件**

**流程**
1. 生成假设陈述（可测量、有方向性）
2. 操作化定义：每个抽象概念映射到可测量指标
3. 变量设计：自变量 / 因变量 / 控制变量 / 混淆因素
4. **可证伪性检查（确定性）**：能否写出"什么观察结果会否定它"？写不出则判定不可证伪，退回重生成
5. **变量完备性检查（确定性）**：四类变量是否齐全；混淆因素是否识别
6. 假设演化树：记录 `parent_id`，保留被修改/放弃的假设及其原因
7. ★ **对抗评审 + 人工确认**

**确定性组件**：可证伪性检查器、变量完备性检查器。

### 7.4 S4 Designer —— 实验设计

**输入**：假设、变量表、领域包
**输出**：可执行实验方案 + 设计检查清单

**流程**
1. 设计类型选择（对照实验 / 消融 / 敏感性分析 / 交叉验证 / 大规模基准）
2. 对照组与基线选择（须引用文献中的既有基线）
3. **样本量估算（确定性公式）**：效应量、显著性水平、检验力、脱落率 → 输出所需样本量或运行次数
4. 指标选择：从领域包 `auto_evaluable_metrics` 中选，**必须全部可自动计算**
5. 消融与敏感性分析设计
6. 计算代价估算（算力、时长、成本）
7. **设计模式检查器（确定性）**：有无对照组？有无混淆？是否可复现（种子/环境是否确定）？指标是否客观？
8. ★ **人工确认方案**

**确定性组件**：样本量估算器、设计模式检查器。

### 7.5 S5 Executor —— 条件执行（全系统重点）

#### 7.5.1 核心原则

1. **判定必须是显式步骤**，有依据、可解释、可被用户覆盖——不是隐含假设
2. **两个分支的产出在报告中必须视觉可分**，绝不能让人把"方案"当成"结果"
3. 判定输入来自 S4 实验方案，结果写入 `feasibility_assessments`

#### 7.5.2 第 1 步：研究类型判定

| 研究类型 | 典型形态 | 默认判定 |
|---|---|---|
| `computational` 计算/算法实验 | 算法对比、模型训练、性能测试 | **可接入** |
| `data_driven` 数据驱动分析 | 统计建模、数据挖掘 | **可接入** |
| `simulation` 数值仿真 | 流体/力学/电路/排队论 | **可接入** |
| `formal_verification` 形式化验证 | 定理证明、模型检验 | **可接入** |
| `meta_analysis` 元分析/系统综述 | 对已提取数据做效应量合并 | **可接入**（见 §7.5.6） |
| `wet_lab` 湿实验 | 化学合成、细胞、动物实验 | 不可接入 → 方案交付 |
| `clinical` 临床/人因 | 临床试验、用户研究 | 不可接入 → 方案交付（+ 伦理提示） |
| `field` 田野调查 | 实地观察、访谈 | 不可接入 → 方案交付 |
| `survey` 问卷调查 | 量表设计与发放 | 不可接入 → 方案交付 |

#### 7.5.3 第 2 步：五道可行性检查

| # | 检查项 | 判断方式 | 不通过后果 |
|:---:|---|---|---|
| ① | **实验对象可被代码操作** | 方案中是否存在"需要物理实体"的动作 | 不可接入 |
| ② | **数据可得** | 所需数据集是否可访问，**并实际探测一次** | 不可接入 |
| ③ | **计算资源足够** | 算力需求 vs 本机/可申请资源 | 可降级（缩小规模后接入） |
| ④ | **结果可自动评价** | **最关键**：是否存在可自动计算的客观指标（领域包 `auto_evaluable_metrics`） | 不可接入——**无客观指标就无法自动迭代** |
| ⑤ | **不涉伦理敏感** | 是否涉及人类受试者/动物/隐私数据 | 不可接入 + 强制伦理提示 |

**判定输出示例**

```json
{
  "research_type": "computational",
  "verdict": "executable",
  "checklist": {
    "code_operable":  {"pass": true, "evidence": "算法在公开数据集上的对比，无物理实体操作"},
    "data_available": {"pass": true, "evidence": "探测到本地数据集缓存，SHA256 已校验"},
    "compute_ok":     {"pass": true, "evidence": "需求 24GB 显存，本机 4090 满足；建议 batch 减半"},
    "auto_evaluable": {"pass": true, "evidence": "指标 Top-1/Top-5 准确率，在领域包指标表中"},
    "ethics_clear":   {"pass": true, "evidence": "使用公开数据集，不涉及人类受试者"}
  },
  "reason": "五道检查全部通过，进入真实执行分支"
}
```

#### 7.5.4 分支 A —— 真实执行（可接入 · 计算机类主场景）

```
环境快照（依赖锁 / GPU / CUDA / OS / 随机种子）
   ↓
代码生成（实验脚本）→ 静态检查（语法 / 依赖 / 危险调用 / 数据路径）
   ↓
沙箱执行
   ├ 目录白名单：仅项目 workspace + 数据集只读目录
   ├ 默认禁网（数据集下载走白名单域名，需审批）
   ├ 资源上限：CPU / 内存 / 时长 / GPU 配额
   └ 产物落盘后计算 SHA256
   ↓
失败自愈循环
   ├ 读栈回溯 → 分类：语法 / 依赖 / 数据 / 逻辑 / 超时 / OOM
   ├ 对应修复策略 → 重跑
   └ 同一错误 ≤ 3 次，超过则升级人工（禁止无限重试烧钱）
   ↓
规模化执行
   ├ 超参扫描（并行，统一登记 run_group）
   └ 多种子重复（★ 同一实验 ≥3 个随机种子，报告均值 ± 标准差）
   ↓
结果回收 → execution_runs 全量登记（代码 + 输入哈希 + 种子 + 环境快照）
   ↓
交付：真实指标 + 图表 + 可复现记录
```

> **"多种子重复"是"可复现"的第一道防线**，方案里写死，不可省。

#### 7.5.5 分支 B —— 方案交付（不可接入）

| 交付物 | 内容 |
|---|---|
| 实验协议 / SOP | 设备、材料、步骤、时间点、操作要点 |
| 材料与试剂清单 | 规格、用量、供应商类型、替代方案 |
| **数据采集表模板** | 字段定义 + 单位 + 取值范围 + 校验规则（可直接导入系统） |
| 样本量估算说明 | 方法、参数假设、计算过程（由确定性引擎算，不是模型估） |
| 随机化与盲法方案 | 分组方法、盲法层级、揭盲条件 |
| 质量控制与偏差控制 | 质控点、排除标准、异常值处理预案 |
| 伦理审查提示 | 需提交的伦理材料清单、审查周期 |
| **预登记分析计划** | **实验前就定下**：什么数据、什么检验、什么阈值、如何处理缺失 |

> **预登记分析计划是这条分支最有价值的产出**：它是提高科研质量的重要实践（防 p-hacking），也是本项目"分析必须由确定性引擎执行"的天然延伸——**分析计划先定，结果后到，Agent 无权事后改计划**。

#### 7.5.6 特例：元分析 / 系统综述是可接入的

系统综述与元分析的"执行"就是对已提取的效应量做统计合并，**完全可自动化**：

- 效应量计算（OR / RR / SMD）
- 固定效应与随机效应模型
- 异质性检验（I²、τ²、Q 检验）
- 亚组分析与敏感性分析
- 发表偏倚检验（漏斗图 / Egger 检验）
- GRADE 证据分级

全部是**确定性统计**，可完整登记到 `execution_runs` + `analysis_runs`，产出森林图、漏斗图、异质性报告。

> **这是演示"全链路真跑通"的最佳场景**——端到端 100% 可接入、每一步都有确定性组件、且完全不需要外部环境。**建议作为答辩的主 Demo。**

#### 7.5.7 用户覆盖机制

- 判定结果页必须显示**五道检查的逐项依据**，不只给结论
- 提供申诉入口：用户可声明"数据我确实能拿到""算力我能申请"，强制转执行分支
- 覆盖写入 `overridden_by_user` 与 `override_note`，审计可查

#### 7.5.8 两分支的结果区分（硬性要求）

| 项 | 真实执行 | 方案交付 |
|---|---|---|
| 报告标识 | 绿色徽标「已执行」+ 环境快照 + 种子 | 灰色徽标「方案未执行」+ 判定依据 |
| 数据呈现 | 真实指标 + 图表 + 误差条 | **不放任何"结果表格"**，只放方案与预期 |
| 复现包 | 含可运行代码与数据 | 含协议与采集模板，无代码 |
| 溯源 | 每个数字 → `execution_run` → 代码行 | 每个步骤 → 文献或方法学依据 |

### 7.6 S6 Analyst —— 分析解读

**输入**：执行结果（真实）或交接包（方案）、领域包
**输出**：分析结果 + 图表 + 与文献对比表

**流程**
1. **统计检验选择（确定性规则引擎）**：按数据类型（连续/分类/计数/生存）、分布假设、样本量、实验设计、配对与否 → 选择检验方法，**并输出选择依据**
2. 执行检验（scipy / statsmodels），计算统计量、p 值、效应量、置信区间
3. 多重比较校正（Bonferroni / FDR）
4. 图表生成（matplotlib / plotly），带误差条
5. 结果与文献对比（引用 S2 的既有结果）
6. **结论-统计一致性校验（Critic）**：文本结论与统计结果是否矛盾；是否把"无显著差异"表述成"没有影响"；是否把相关性表述成因果
7. ★ 人工解读确认

**确定性组件**：检验选择规则引擎、效应量计算、多重比较校正、图表生成。**所有数字来自代码，模型只负责解释。**

### 7.7 S7 Writer —— 写作成稿

**输入**：全链路产物
**输出**：带证据锚点的结构化草稿

**流程**
1. 按领域包 `report_standard` 生成骨架（计算机类默认 IMRaD，生物医学可加 PRISMA）
2. 逐节起草，**每个论断必须绑定证据锚点**（引用切片 id / 执行结果 id / 分析结果 id）
3. 引用插入 + CSL 格式化（按领域包的 `reference_style`）
4. 术语一致性检查（依领域包术语表）
5. **引用真实性校验（确定性）**：锚点必须存在于本项目上下文，否则该论断降级或退回补证据
6. 图表插入与交叉引用检查
7. ★ 人工审阅

> **不生成"成稿"**，只生成"带证据锚点与占位标记的结构化草稿"——作者必须自己过一遍并署名。

### 7.8 S8 Publisher —— 投稿与复现

**输入**：草稿、执行结果、领域包
**输出**：复现包 + 投稿材料清单 + 评审响应框架

**流程**
1. 期刊/会议匹配（基于领域包 `venues.yaml` + 摘要相似度 + 影响因子与周期）
2. 投稿材料清单生成（正文、图表、附录、数据可用性声明、作者贡献、利益冲突）
3. **复现包打包**：代码 + 数据引用 + 环境锁 + 随机种子 + README + 一键复现脚本
4. **复现包校验器（确定性）**：在干净环境模拟运行，检查是否可跑通；不可跑通则标记并给出缺失项
5. 评审意见响应的结构化整理（**不自动生成答复措辞**，只做结构化组织）
6. **不自动投递**——所有对外动作需人工执行

---

## 8. AI 接入层

### 8.1 Provider 抽象与注册表

`ChatProvider` / `EmbeddingProvider` / `RerankProvider` / `ParseProvider`，统一由 `ProviderRegistry` 出口。每个 Provider 声明 `capabilities` 与 `health()`。

**健康为三态**：`ok` / `unconfigured` / `down`。注册表保留全部已声明的 provider，**不按健康状态剔除**；可用性在调用点由 `check_available()` 判定，未达标时抛出的错误必须携带可操作提示（如「请先在设置页录入 API Key」）。

**Provider 类型**

| 类型 | 说明 | 端点 |
|---|---|---|
| `mock` | 确定性假模型，零配置首启与测试用 | 进程内 |
| `openai_compat` | OpenAI 兼容 HTTP 端点，`base_url` 可指向任意兼容网关 | `POST {base_url}/chat/completions` |
| `anthropic_compat` | Anthropic Messages 协议，视需要启用 | `POST {base_url}/v1/messages` |

**用户自定义 Provider 接入**

用户可在设置页新增、修改、删除任意模型后端，不改代码、不重启。

| 字段 | 必填 | 说明 |
|---|:---:|---|
| `name` | ✅ | 唯一标识，被档位路由与 Agent 定义引用 |
| `type` | ✅ | 协议类型，取自上表 |
| `base_url` | ✅ | 端点根地址；校验禁内网网段、禁跟随重定向（§10.5） |
| `models` | ✅ | 该后端可用的模型 ID 列表 |
| `vendor` | ✅ | 厂商标识，用于 `critique` 跨厂商校验（§8.2）；自建端点如实填写 |
| `api_key_ref` | ⬜ | 凭据管理器中的引用名；本地端点（如 Ollama）可留空 |
| `capabilities` | ✅ | 声明 `json_object` / `tools` / `stream` / `vision`；未声明的能力按缺失处理（§8.3） |
| `price` | ⬜ | 每 1K token 输入 / 输出单价，用于成本归因；留空按 0 记账 |
| `timeout_s` | ⬜ | 覆盖默认超时（§11.4） |
| `extra_headers` / `extra_body` | ⬜ | 透传字段，兼容需要特殊参数的端点 |

**模型清单获取**：端点若兼容 `GET {base_url}/models`，提供「自动拉取」按钮列出候选模型；不兼容时用户手工填写。

**能力与价格不由上游推断**：端点返回的模型清单只取 `id` 列表；`capabilities` 与单价一律以本地配置为准，不依赖上游返回值。

**写入即生效**：新增或修改 provider 后立即重建注册表，并失效该 provider 的健康探测缓存，无需重启。

### 8.2 档位：按科研动作划分

| 档位 | 典型任务 | 质量敏感度 | 调用量 | 默认后端 |
|---|---|---|---|---|
| `extract` | 字段抽取、分类、打标、实体识别 | 低 | 极高 | 用户指定的轻量模型 |
| `plan` | 研究问题生成、假设生成、实验方案设计 | 高 | 中 | 用户指定的强模型 |
| `critique` | 评审、证据校验、一致性检查 | 高 | 中 | **与产出方不同厂商的模型** |
| `synthesize` | 分析解读、综述合成、投稿匹配 | 高 | 低 | 用户指定的强模型 |
| `write` | 结构化草稿生成 | 中 | 中 | 用户指定的中档模型 |

**关键约束**：`critique` 档必须配置为与 `plan` / `synthesize` **不同厂商**的模型，否则失去交叉验证意义。厂商取自 provider 配置的 `vendor` 字段（§8.1），配置加载期校验。

**档位绑定的是模型能力，不是模型来源或价格**。用户可为每个档位指定任意已配置的 provider / model。`plan` / `critique` / `synthesize` 的产出直接构成结论，必须由用户在首次运行时显式指定；指定时界面显示能力提示，并把该选择写入 `provider_switch_log` 供审计。**任何档位未配置时该档位不可用，界面明确提示，不静默回落到任何默认模型。**

### 8.3 能力矩阵与降级

| 缺失能力 | 降级动作 |
|---|---|
| JSON Schema 强约束 | 注入 schema 至 system prompt + `json_object` + 失败时携带错误重试一次 |
| 原生工具调用 | 切换 ReAct 文本协议（Action / Action Input） |
| 流式工具调用 | 工具调用场景退回非流式 |
| 稀疏向量 | 检索降级为 dense + FTS5 双路，记录降级事件并告警 |
| 上下文不足 | 切 Map-Reduce 分段处理 |
| 能力未声明或上游未返回能力字段 | 按最小能力集处理，首次调用探测实际支持情况并回写 `llm_usage.degraded` |

所有降级写入 `degraded` 字段并记录审计。

**协议一致性校验**：路由候选链中的每个 `(provider, model)` 必须指向其 `type` 对应的协议端点。`openai_compat` 类型的 provider 只允许挂载支持 Chat Completions 协议的模型；走 Messages、Responses 等其他协议的模型须单独建一个对应 `type` 的 provider。配置加载期校验并拒绝不匹配的组合。

### 8.4 运行时热切换

| 粒度 | 生效范围 | 操作 | 需重启 |
|---|---|---|---|
| 请求级 | 单次调用 | 请求体 `provider` / `model` | 否 |
| Agent 级 | 单个 Agent 定义 | `PATCH /api/agents/{id}` | 否 |
| 档位级 | 某一档位 | `PATCH /api/settings/routing` | 否 |
| 全局级 | 所有新建项目 | `POST /api/settings/providers/reload` | 否 |

**Provider 管理 API**

| 操作 | 端点 |
|---|---|
| 列出（含健康状态） | `GET /api/settings/providers` |
| 新增 | `POST /api/settings/providers` |
| 修改 | `PATCH /api/settings/providers/{name}` |
| 删除 | `DELETE /api/settings/providers/{name}` |
| 录入 / 更新密钥 | `PUT /api/settings/providers/{name}/key` |
| 探测端点可用模型 | `POST /api/settings/providers/{name}/probe-models` |
| 热重载全部 | `POST /api/settings/providers/reload` |

**切换保障**：项目启动时冻结 `RegistrySnapshot`，切换只影响新项目；配置加载阶段拦截路由引用错误与能力缺失；每次切换写 `provider_switch_log`。

API Key 存 **Windows 凭据管理器**（`keyring`）；`config.yaml` 只存 `api_key_ref` 引用名，不落盘明文。未录入 Key 的 provider 健康为 `unconfigured`：注册表保留，调用点拒绝并提示「请先在设置页录入 Key」。录入 Key 后主动失效该 provider 的健康探测缓存，无需重启。

### 8.5 成本控制（多 Agent 场景必须做）

| 机制 | 参数 |
|---|---|
| **项目级预算熔断** | 单项目总额（如 ¥50）+ 每日额度，超限即暂停并请人确认 |
| **Agent 级预算** | 每 Agent 独立步数与成本上限（如 Scout 20 步 / ¥2，Writer 40 步 / ¥5） |
| **同阶段重试上限** | 同一阶段 ≤ 3 次；同一子目标 ≤ 3 次（防振荡死循环） |
| **跨 Agent 缓存** | `(provider, model, messages_hash, schema)` 命中即复用，多 Agent 场景命中率高；缓存落 SQLite 表并设 TTL，跨重启有效 |
| **档位分级** | 简单步骤走轻量档；只有 `plan` / `critique` / `synthesize` 用强档 |
| **思考预算** | Critic 反思轮次独立上限 |
| **成本归因** | 按项目 / 阶段 / Agent 三维展示，一眼看出谁最烧钱 |
| **价格表** | 每 1K token 输入 / 输出单价来自用户对各 provider 的配置（§8.1），不依赖上游返回值；单价为 0 时仍如实记账并标注 provider / model |

**成本目标**：单项目全链路（含 30 篇文献 + 一轮真实实验）< ¥15。

---

## 9. 数据模型

核心表（SQLite，WAL 模式，`foreign_keys=ON`）：

| 分组 | 表 | 说明 |
|---|---|---|
| **项目** | `projects` | 研究项目容器（标题、领域、目标、状态） |
| | `decisions` | 决策日志（阶段、决策、理由、备选方案、决策人） |
| | `stage_checkpoints` | 阶段检查点，支持单阶段重跑 |
| **Agent** | `agents` | Agent 定义与配置（角色、档位、工具、启用状态） |
| | `agent_runs` | Agent 运行轨迹（阶段、状态、步数、成本） |
| | `agent_steps` | 单步明细（思考、工具、参数、结果、决策） |
| **Agent 内核** | `conversations` | 会话（所属项目、标题、状态） |
| | `messages` | 消息（角色 user / assistant / tool / system、内容、工具调用关联、token 数） |
| | `task_plans` | 结构化任务计划（版本、状态、模式 plan_execute / react、步骤 JSON） |
| | `tool_calls` | 工具调用（工具名、参数、权限等级、结果、错误、关联审批单、耗时） |
| | `kernel_checkpoints` | 内核检查点（计划、步骤序号、快照），支持中断后恢复 |
| **黑板** | `blackboard` | 结构化共享状态（类型、版本、载荷、产出 Agent、证据） |
| **阶段产物** | `research_questions` | 研究问题 + 空白点证据 + 可行性评分 |
| | `hypotheses` | 假设 + 操作化定义 + 变量 + 否证条件 + 状态 + 演化树 |
| | `experiment_plans` | 实验方案 + 样本量 + 指标 + 设计检查结果 |
| | `drafts` | 写作草稿 + 段落级证据锚点 + 版本 |
| **执行** | `feasibility_assessments` | ★ 接入判定（研究类型、结论、五道检查、依据、用户覆盖） |
| | `execution_runs` | ★ 真实执行（代码、输入哈希、随机种子、**环境快照**、指标、产物） |
| | `analysis_runs` | ★ 统计分析（选检验依据、代码、结果、图表） |
| | `handover_packs` | 交接包（协议、SOP、采集表、样本量说明、盲法、伦理、预登记分析计划） |
| **文献** | `documents` / `chunks` / `chunks_fts` / `chunks_fts_tri` | 文献、切片、FTS5 索引（unicode61 + trigram） |
| | `doc_relations` | 图谱关系（CITES / AUTHORED / AFFILIATED_WITH / PROPOSES / APPLIES / EVALUATED_ON / IMPROVES_UPON / LIMITATION） |
| | `search_strategies` | ★ 可复现检索策略记录（检索式、时间、结果数） |
| **任务与成本** | `jobs` / `job_events` | 后台任务与事件；事件按 `seq` 递增，SSE 据此支持 `Last-Event-ID` 断线续传 |
| | `llm_usage` | 调用记账（**含 project_id / stage / agent_id**，支持三维归因） |
| | `llm_cache` | LLM 响应缓存（cache_key、provider、model、tier、响应、过期时间） |
| | `app_config` | 运行期状态持久化（熔断状态等） |
| | `budget_grants` | 预算豁免（审批签发的额度、有效期、关联审批单） |
| | `provider_switch_log` | 后端切换审计 |
| **产出** | `artifacts` | 复现包与产物（路径、哈希、可复现标记、校验日志） |

**三个可复现性关键字段**（必须落库，不可省）：
- `execution_runs.env_snapshot` —— 依赖锁、运行时版本、GPU、CUDA、OS
- `execution_runs.random_seed` —— 随机种子
- `analysis_runs.code_hash` + `input_hash` —— 代码与输入的哈希

有了这三项，同一实验才能真正重跑出相同结果，"计算可复现率 100%" 才有依据。

---

## 10. 安全与治理

### 10.1 Agent 行为安全（本系统新增的核心安全面）

| 措施 | 说明 |
|---|---|
| **工具权限分级** | 只读 / 写草稿 / 写正式 / 删除 / 出网，默认最小权限 |
| **危险操作强制事前审批** | 删除文件、覆盖已有数据、Git push、对外发送——**不可配置关闭** |
| **写操作全审计 + 自动快照** | 每次写入前生成快照，可一键回滚 |
| **沙箱约束** | 目录白名单 + 默认禁网 + 资源配额 + 非 root + 只读根文件系统 |
| **产物哈希** | 所有实验产物落盘后计算 SHA256，防篡改 |

### 10.2 研究伦理护栏

| 场景 | 处置 |
|---|---|
| 请求伪造/篡改实验数据 | **直接拒绝**，记录事件 |
| 统计结果与结论文本矛盾 | Critic 强制拦截，退回 S6 |
| 涉及人类受试者/动物 | 强制伦理审查提示，S5 判定为不可接入 |
| 请求生成虚假引用 | 引用真实性校验器拦截 |
| 请求绕过同行评审流程 | 不支持，无此功能 |

### 10.3 提示注入五层防护

| 层 | 措施 |
|---|---|
| 输入隔离 | 文献正文以 `<untrusted_document>` 包裹；system prompt 声明标签内为不可信数据 |
| 启发式检测 | 匹配注入特征，标记并剥离，写入 `job_events` |
| 权限最小化 | 处理文献内容的 Agent 不持有文件写权限、不持有网络工具、不持有密钥 |
| 输出隔离 | 模型输出视为数据，进入界面前经 DOMPurify 净化 |
| 溯源强制 | 所有论断须附可校验引用坐标，无引用者降级呈现 |

### 10.4 文件上传与沙箱

- 上传：大小上限 50 MB，页数上限 200，MIME 双重校验（扩展名白名单 + `%PDF-` 魔数）
- 解析在**独立子进程**执行并设内存上限，超限终止
- 存储名用 `sha256`，原始文件名不参与路径拼接
- 沙箱：受限令牌 + 目录白名单 + Job Object 资源限制 + 默认禁网；GPU 配额可选
- **Windows 上没有轻量容器，沙箱是真实技术难点**——备选方案：受限令牌 + 独立用户账户；或 WSL2（需用户环境支持，作为可选增强）

### 10.5 访问控制

| 项 | 方案 |
|---|---|
| 绑定 | `127.0.0.1`，不监听外部网卡（本机回环不构成安全边界） |
| 令牌 | 启动生成 32 字节随机值，注入首屏 URL，API 校验 `X-App-Token` |
| 应用锁 | 可选，Argon2id（`time_cost=3, memory_cost=64MB, parallelism=4`） |
| 外部链接 | 界面内打开前需用户确认，防钓鱼跳转 |
| SSRF | `base_url` 可配置但须校验：禁止内网网段、禁止跟随重定向 |

---

## 11. 错误处理与容错

### 11.1 分层

```
界面层   ErrorBoundary · HTTP 拦截器统一处理 · SSE 断线自动重连（Last-Event-ID）
服务层   校验失败 → 4xx + 错误码 · 业务异常 → AppError（码 + 文案 + 处置建议）· 兜底 5xx 不泄漏堆栈
编排层   ★ Agent 级自纠：产物不合格 → 回退上游重做（不是重试）
任务层   步骤级捕获写 job_events · 单条目失败不影响整体，任务标记 partial
执行层   ★ 实验失败 → 读栈回溯 → 分类 → 对应修复 → 重跑，同一错误 ≤3 次
外部调用 统一 HTTP 客户端：超时、重试、退避、熔断、脱敏日志
```

### 11.2 Agent 级自纠错误类别（本系统特有）

| 类别 | 处置 |
|---|---|
| 论断无证据支持 | 回退到 S2 补检索 |
| 统计与结论文本矛盾 | 回退到 S6 重做解读 |
| 引用不存在 | 回退到 S7 重新绑定锚点 |
| 实验设计缺对照组 | 回退到 S4 重设计 |
| 实验代码反复失败（>3 次） | 升级人工，附完整日志与尝试记录 |
| 假设不可证伪 | 回退到 S3 重新生成 |

### 11.3 错误码格式

`<域>-<类别>-<序号>`，域名：`APP` `STORE` `LLM` `SEARCH` `PARSE` `JOB` `AGENT` `EXEC` `DATA`。
示例：`LLM-QUOTA-002`、`EXEC-SANDBOX-001`、`AGENT-LOOP-003`、`PARSE-QUAL-003`。

### 11.4 重试与熔断

| 项 | 规则 |
|---|---|
| 可重试判定 | 按 HTTP 状态与异常类型；参数错误、配额不足不重试 |
| 鉴权失败（401 / 403） | **不重试、不计入熔断**；把该 provider 标记为 `unconfigured`，提示「请先在设置页录入 Key」 |
| 退避 | 指数退避 + 抖动，基准 1 s，最多 3 次 |
| 熔断 | 单后端连续失败 5 次进入冷却，冷却 5 分钟，期间走降级链；状态持久化到 `app_config`，重启后保留；热重载不清零未变更 provider 的计数 |
| 超时 | 模型调用 120 s，检索 15 s，解析 300 s，**实验执行按方案配置（默认 3600 s）** |
| 幂等 | 重试使用同一幂等键，写库 upsert |

### 11.5 离线与降级

模型后端不可用时，本地功能保持可用：文献库浏览、笔记批注、已解析内容检索、向量检索降级为 FTS5。**真实执行分支不受影响**（沙箱是本地能力）。界面顶部显示离线提示，恢复后由用户手动触发重跑，不自动重跑。

**两类不可用须区分提示**：① `unconfigured`（未录入 Key 或 Key 失效）→ 提示「请先在设置页录入 API Key」，并给出跳转入口；② `down`（网络不可达或上游故障）→ 提示离线。二者都不自动重跑。

---

## 12. 评估方案

### 12.1 时间切片金标法（端到端评测核心）

- **做法**：取 2023 年及以前发表的论文作为"当时的知识状态"，把该论文**真实的研究问题 / 方法 / 实验设计**当标准答案，让 Agent 仅基于 2023 年前文献独立产出下一步研究方案
- **指标**：研究问题重合度、方法选择命中率、数据集与指标选择命中率
- **优势**：金标**天然存在、零标注成本**（arXiv 有完整时间线）

### 12.2 阶段级指标

| 阶段 | 指标 | 目标 |
|:---:|---|---|
| S1 | 候选问题被后续真实论文覆盖的比例 | ≥ 30% |
| S2 | nDCG@10 / 引用可溯源率 / 去重准确率 | 80% / 95% / 98% |
| S3 | **可证伪率** / 变量完备率 | **100%** / ≥ 90% |
| S4 | 样本量估算与标准方法一致性 / 设计检查通过率 | ≥ 95% / ≥ 90% |
| **S5** | **执行成功率 / 自愈成功率 / 多种子结果一致性（变异系数）/ 判定准确率** | ≥ 85% / ≥ 70% / CV ≤ 5% / ≥ 95% |
| S6 | 检验选择正确率（对比统计教材规则）/ 结论-统计一致性 | ≥ 90% / **100%** |
| S7 | **引用真实性** / 术语一致率 | **100%** / ≥ 95% |
| S8 | 复现包可跑通率 | ≥ 90% |

### 12.3 反事实测试

| 测试 | 检验什么 |
|---|---|
| 注入与结论矛盾的文献 | Agent 是否盲从、是否坚持证据 |
| 注入伪造实验数据 | 伦理护栏是否拦截 |
| 注入不存在的引用 | 引用校验器是否拦截 |
| 移除对照组 | 设计检查器是否发现 |

### 12.4 消融实验

| 维度 | 取值 |
|---|---|
| 编排模式 | 单 Agent 循环 / 固定流水线 / Supervisor+Blackboard |
| 评审机制 | 无 Critic / 同源 Critic / 异构 Critic |
| 执行分支 | 仅方案交付 / 条件执行 |
| 溯源约束 | 无溯源 / 强制溯源 |

**输出**：质量（金标命中率 + 五项核心指标）× 延迟 × 成本。

### 12.5 模型组件测试

| 类型 | 做法 |
|---|---|
| Golden Test | 固定输入 + 固定断言；`temperature=0`；关键字段完全匹配 + 长文本语义相似度 ≥0.85 |
| Schema Test | 每个 `response_schema` 运行 50 次，解析成功率 ≥98% |
| Judge 校准 | 先在 ≥50 条人工标注上测对齐率并报告 Cohen's κ；κ < 0.6 则弃用该 Judge |
| **确定性组件单测** | 样本量估算、检验选择、CSL 格式化、引用校验、引用真实性——**必须有单元测试，覆盖率 ≥ 90%** |
| 回归测试 | 修改 Prompt 后运行全量 golden set |

---

## 13. 可观测

| 层次 | 内容 |
|---|---|
| **Agent 轨迹** | 每步的输入 / 思考 / 工具调用 / 结果 / 决策理由 / 成本，**支持回放** |
| LLM Trace | 每步输入输出、耗时、token、成本、`degraded` |
| 本地日志 | structlog → JSON 文件，`trace_id` 贯穿三层，字段脱敏，按天滚动保留 14 天 |
| 阶段级成本 | 按项目 / 阶段 / Agent 三维展示 |
| 界面指标 | 任务队列状态、近 7 日用量与成本、解析成功率、执行成功率、错误码分布 |
| 诊断包 | 一键导出 zip：版本、系统信息、配置快照（密钥脱敏）、近 7 天日志、近 20 条任务状态、DB 完整性检查、磁盘剩余 |

> **科研场景下"为什么得出这个结论"比"耗时多少"更重要**——所以 Agent 轨迹是一等公民。

---

## 14. 性能与成本指标

| # | 指标 | 目标 |
|---|---|---|
| 1 | 冷启动至界面可用 | < 3 s |
| 2 | 检索 API P95 | < 1.5 s（缓存命中 < 300 ms） |
| 3 | 单篇解析（有文本层，≤30 页） | < 20 s |
| 4 | 单篇解析（扫描件） | < 120 s |
| 5 | 30 篇文献综述阶段端到端 | < 12 min |
| 6 | 问答首 token 延迟 | < 2 s |
| 7 | 引用可溯源率 | ≥ 95% |
| 8 | **计算可复现率** | **100%** |
| 9 | **执行真实性标注率** | **100%** |
| 10 | 结构化抽取成功率（含重试） | ≥ 98% |
| 11 | 真实执行成功率（计算类） | ≥ 85% |
| 12 | 实验失败自愈成功率 | ≥ 70% |
| 13 | 内存常驻（空闲） | < 500 MB |
| 14 | **单项目全链路成本**（30 篇 + 一轮实验） | **< ¥15** |
| 15 | 模型热切换耗时 | < 3 s，在途任务零中断 |
| 16 | 未捕获异常导致崩溃 | 0 次 / 100 小时 |
| 17 | 任务失败后可自动恢复比例 | ≥ 80% |
| 18 | 单条目失败影响整体任务的次数 | 0 |

---

## 15. 工程结构

```
researchpilot/
├── backend/
│   ├── app/
│   │   ├── agent_kernel/   # ★ Agent 内核：循环、任务拆解、工具注册、权限闸门、上下文
│   │   ├── api/            # 路由、静态托管、SSE
│   │   ├── orchestration/  # ★ Orchestrator、阶段调度、回退边、检查点
│   │   ├── agents/         # 8 阶段 Agent + 4 支撑 Agent
│   │   ├── domain/         # ★ 领域包加载、校验、规则求值
│   │   ├── ai/             # base.py registry.py providers/ budget.py
│   │   ├── store/          # SQLite 连接、迁移、DAO
│   │   ├── pipeline/       # 解析路由、章节切分、分块、向量化
│   │   ├── search/         # 聚合、去重、排序、混合检索
│   │   ├── exec/           # ★ 沙箱调度、环境快照、失败分类、自愈
│   │   ├── analysis/       # ★ 检验选择引擎、效应量、绘图
│   │   ├── graph/          # 图谱递归查询、空白点扫描
│   │   ├── jobs/           # Worker、进度、断点续跑
│   │   ├── security/       # 令牌、脱敏、上传校验、注入检测、伦理护栏
│   │   └── observability/
│   ├── migrations/  tests/  pyproject.toml
├── domain_packs/
│   └── cs-ai/              # ★ 内置领域包
├── frontend/src/{pages,components,api,stores}/
├── launcher/               # 启动器、托盘、打包脚本
├── config/default.yaml
├── eval/{goldset/,scripts/,reports/}
├── docs/adr/
└── README.md  LICENSE  NOTICE  THIRD_PARTY_LICENSES.md
```

---

## 16. 开发过程

采用 **Scrum，2 周一个 Sprint，共 9 个 Sprint（18 周）**，分两个阶段推进：阶段一交付 Agent 内核，阶段二在核之上扩展科研技能。

完整的用户故事、故事点、Sprint 编排、决策闸口、完成的定义与裁剪规则见独立文档：**《修订排期与修复计划》**。

---

## 17. 风险清单

| 风险 | 概率 | 应对 |
|---|---|---|
| **范围失控**（8 阶段各是专业领域） | 高 | 深度分层（S2/S4/S6 做深）；S5 条件执行；S8 不自动投递 |
| **多 Agent 互相吹捧导致虚假交付** | 高 | Critic 独立于产出链 + **异构模型** + 有否决权 |
| **成本指数放大** | 高 | 项目级 + Agent 级双重熔断；同目标重试 ≤3 次 |
| **循环振荡**（Critic 否 → 重做 → 又否） | 中高 | 同阶段重试上限；超限强制人工介入；Curator 记录失败原因避免重复路径 |
| Windows 沙箱能力有限 | 中高 | 受限令牌 + 目录白名单 + Job Object；WSL2 作为可选增强；**明确记录能力边界，不宣称强隔离** |
| 实验执行环境差异导致不可复现 | 中 | 环境快照强制记录；多种子重复；复现包校验器 |
| **执行真实性被误读**（把方案当结果） | 中 | 两分支徽标强制区分；方案分支不放结果表格 |
| 责任模糊（出错不知谁的锅） | 中 | 全程轨迹 + 每产物标注产出 Agent |
| 上下文爆炸 | 中 | 分层摘要 + 只传相关切片；黑板存结构化对象 |
| 理解漂移 | 中 | 项目级术语表 + 结构化黑板 + 关键概念强制引用统一 ID |
| 找不到行业访谈对象 | 低 | 计算机类可直接访谈同门/实验室师兄师姐 |
| 时间切片金标构造超预期 | 中 | 先用 20 篇小规模验证方法可行性，再扩到 50 篇 |

---

## 18. 交付与许可

**交付物**：桌面安装包与免安装包、前后端与启动器源码、编排层与 Agent 实现、领域包（含 cs-ai）、数据库迁移脚本、依赖许可清单、需求规格说明书、架构设计说明书、ADR、安全设计说明书、接口文档、部署与使用手册、评估报告、演示视频、示例数据集、**示例复现包**。

**许可**：Apache-2.0 发布。依赖按三级许可分级管理：

| 级别 | 定义 | 允许形态 |
|:---:|------|---------|
| L1 | MIT / Apache-2.0 / BSD / PSF | 代码依赖 |
| L2 | GPL / AGPL / CC-BY-NC | 仅独立进程或远端服务 |
| L3 | 闭源但开发者免费 | 仅外部服务调用 |

CI 许可检查：`pip-licenses` + `license-checker` + CycloneDX SBOM，配置 GPL / AGPL / SSPL 黑名单拦截。

示例数据集仅使用 CC-BY / CC0 许可的开放获取文献。领域包中的术语表与规则库若引用外部公开标准，在 `NOTICE` 中标注来源。
