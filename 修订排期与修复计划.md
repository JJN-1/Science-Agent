# 科研 Agent 分阶段实施与冲刺排期（修订版）

**针对**：ResearchPilot（`researchpilot/`）
**修订日期**：2026-09-19
**周期**：2026-09-21 ~ 2027-01-24，18 周，9 个冲刺 × 2 周
**本文范围**：总体路线 → 工作量与速率 → 技术方案 → 冲刺排期 → 冲刺待办 → 决策闸口 → 裁剪方案 → 执行顺序 → 技术债修复清单

---

## 1. 总体路线

分两个阶段推进，架构分四层。

| 层 | 归属 | 建设周期 | 内容 |
|---|---|---|---|
| 交互层 | 阶段一 | Sprint 3–4 | 会话流、流式反馈、计划卡片、工具调用卡片、人工批准 |
| Agent 内核 | 阶段一 | Sprint 4 | 任务拆解、工具注册与调用循环、权限分级与最小沙箱、上下文管理 |
| 底座 | 已完成 · Sprint 3 收尾 | Sprint 3 | Provider 路由与降级、预算与减免审批、成本归因、结构化黑板、检查点、决策审计、作业与 SSE |
| 技能层 | 阶段二 | Sprint 5–11 | S1–S8 科研阶段与领域工具 |

推进顺序：**底座收尾 → Agent 内核 → 科研技能层**，每层独立可交付。

**阶段一边界**：对话交互、任务理解与拆解、工具调用、多步执行与结果反馈、权限分级与最小沙箱、会话生命周期。不含科研领域知识、多 Agent 协商、插件市场、桌面打包。

**阶段二边界**：领域包机制、S1–S8 阶段技能化、科研专用工具、可复现性字段落点、Critic 异构评审、金标评测与交付。

---

## 2. 工作量与速率

| 分类 | 项 | SP |
|---|---|---:|
| 阶段一前 | 技术债清偿 + 异步地基 + 纵向切片 + 自定义模型接入（Sprint 3） | 56 |
| 阶段一 | Agent 内核（Sprint 4） | 48 |
| 阶段二 | S2 文献主线：检索聚合 / 解析管线 / 检索增强 / 精读溯源 | 60 |
| 阶段二 | S1 研究知识图谱与空白点扫描 | 8 |
| 阶段二 | 领域包机制 + `cs-ai` 包 | 10 |
| 阶段二 | S3 假设形式化 | 25 |
| 阶段二 | Critic 异构评审 + 风险分级人工检查点 | 18 |
| 阶段二 | S4 实验设计 | 25 |
| 阶段二 | S5 判定 + 完整沙箱 + 真实执行 | 45 |
| 阶段二 | S6 分析解读 | 30 |
| 阶段二 | S7 写作成稿 | 26 |
| 阶段二 | S8 投稿复现 | 20 |
| 阶段二 | 回退边 + 编排闭环 | 15 |
| 阶段二 | 评测：时间切片金标 + 反事实 + 消融 | 20 |
| 阶段二 | 成本与性能优化 | 10 |
| 阶段二 | 文档 / 开源规范 / 打包 / 演示视频 / 答辩 | 44 |
| **合计** | | **442** |

**速率**：442 SP ÷ 9 冲刺 ≈ **49 SP/冲刺**。SP 仅用于相对排序与优先级，不作工时换算。

---

## 3. 技术方案

### 3.1 代码结构增量

```
backend/app/
  agent_kernel/                 # 阶段一新增
    loop.py                     # ReAct / Plan-Execute 双模式循环
    planner.py                  # 任务拆解与结构化计划
    context.py                  # 上下文装配与裁剪
    permissions.py              # 权限分级与闸门
    tools/
      registry.py               # 工具注册表
      base.py                   # ToolSpec / Tool / ToolResult 契约
      builtin/                  # 内置工具集
  api/
    chat.py                     # 会话与消息
    plans.py                    # 任务计划
    tools.py                    # 工具清单
    jobs.py                     # 作业受理与 SSE 流
domain_packs/cs-ai/             # 阶段二新增
docs/adr/                       # 架构决策记录
.github/workflows/ci.yml        # 持续集成
```

### 3.2 数据模型

**Sprint 3（migration 4）**

| 表 | 关键字段 |
|---|---|
| `jobs` | id、project_id、kind(`stage`/`pipeline`/`chat`)、stage_id、status(`queued`/`running`/`succeeded`/`failed`/`paused`)、run_id、error、params(JSON)、created_at、started_at、finished_at |
| `job_events` | id、job_id、seq（job 内自增）、type、payload(JSON)、created_at |
| `app_config` | key(PK)、value(JSON)、updated_at |
| `llm_cache` | cache_key(PK)、provider、model、tier、response(JSON)、created_at、expires_at |

**Sprint 4（阶段一，migration 5）**

| 表 | 关键字段 |
|---|---|
| `conversations` | id、project_id、title、status(`active`/`archived`)、created_at、updated_at |
| `messages` | id、conversation_id、role(`user`/`assistant`/`tool`/`system`)、content、tool_call_id、tokens、created_at |
| `task_plans` | id、conversation_id、version、status(`draft`/`approved`/`executing`/`done`/`failed`)、mode(`plan_execute`/`react`)、steps(JSON)、created_at、updated_at |
| `tool_calls` | id、conversation_id、run_id、tool_name、args(JSON)、permission(`read`/`write`/`execute`/`dangerous`)、status、result(JSON)、error、approval_id、duration_ms、created_at |
| `kernel_checkpoints` | id、conversation_id、plan_id、step_index、status、snapshot(JSON)、created_at |

**阶段二（migration 6+，逐冲刺补）**
`research_questions`、`hypotheses`、`experiment_plans`、`feasibility_assessments`、`execution_runs`、`analysis_runs`、`handover_packs`、`drafts`、`artifacts`、`env_snapshot`、`random_seed`。

### 3.3 Agent 内核（阶段一）

**工具契约**　`ToolSpec(name, description, parameters_json_schema, permission, idempotent, timeout_s, result_max_bytes)`；实现 `Tool.run(args, ctx) -> ToolResult(status, output, error, meta)`。

**内置工具集**　`read_file`、`write_file`、`list_dir`、`glob`、`grep`、`run_command`、`http_get`（可按配置关闭）、`run_pipeline`（把 S1–S8 确定性编排注册为一个 capability）。

**调用循环**　单轮流程：装配上下文 → 生成计划或下一步决策 → 若含工具调用则执行 → 结果回填 → 进入下一轮。上限 `kernel.max_steps`（默认 20），达上限转人工。

**双模式**　`plan_execute`：先产出完整结构化计划再逐步执行；`react`：逐步决策。默认 `plan_execute`。

**并行调用**　同一轮内无依赖的工具调用用 `anyio` 任务组并发执行，结果按调用序回填。

**结果处理**　单工具结果超过 `result_max_bytes`（默认 32 KB）时保留头尾并附截断提示。

**错误自愈**　工具失败将错误文本回填给模型继续决策；同一工具同一参数连续失败 3 次则终止该步并请求人工介入。

**权限闸门**

| 等级 | 行为 |
|---|---|
| `read` | 直通，记录 `tool_calls` |
| `write` | 直通，记录并落检查点 |
| `execute` | 首次需人工批准；批准后按工作目录记忆，同目录后续免批 |
| `dangerous` | 每次必须人工批准；含删除、网络写、越出白名单路径 |

批准复用 `approvals` 表，`kind=dangerous`，detail 携带工具名、参数、目标路径。

**最小沙箱**　工作目录白名单；`run_command` 锁定 cwd、设超时、按配置禁网；子进程输出设上限；沙箱内产物记录哈希。

**上下文管理**　按 token 预算装配，裁剪优先级：系统提示 > 当前计划 > 最近 N 轮 > 工具结果摘要 > 更早轮次（摘要化）。

**执行控制**　`max_steps` 上限；预算熔断复用 `BudgetManager`；中断取消后台协程；每步落 `kernel_checkpoints`，支持从中断处恢复。

**确定性模式**　`deterministic=true` 时固定 `plan_execute`、固定编排模板、固定 `seed`、禁用并行、温度置 0。

### 3.4 流式与 API（阶段一）

| 端点 | 说明 |
|---|---|
| `POST /api/conversations` | 新建会话，返回 conversation_id |
| `GET /api/conversations` | 会话列表 |
| `GET /api/conversations/{id}` | 会话详情：消息 + 当前计划 + 工具调用记录 |
| `POST /api/conversations/{id}/messages` | 提交用户消息，立即返回 `202 {job_id}` |
| `GET /api/jobs/{job_id}` | 作业快照（轮询回退用） |
| `GET /api/jobs/{job_id}/stream` | SSE，支持 `Last-Event-ID` 与 `?after_seq=` |
| `GET /api/tools` | 工具清单（含权限等级、幂等性、超时） |
| `PATCH /api/task-plans/{id}` | 人工修改计划步骤后重新执行 |
| `POST /api/jobs/{job_id}/cancel` | 中断执行 |

**SSE 事件类型**：`plan.updated`、`assistant.delta`、`tool.call`、`tool.result`、`approval.required`、`job.succeeded`、`job.failed`、`job.paused`。投递方式为按 `seq` 从 `job_events` 增量读取，每 15s 发心跳。

### 3.5 前端（阶段一）

- 会话界面：消息流 + 计划卡片（可编辑步骤）+ 工具调用卡片（名称 / 参数 / 耗时 / 结果摘要 / 权限徽标）+ 批准卡片
- 订阅 SSE，连续两次失败降级为 1s 轮询 `GET /api/jobs/{id}`
- 自由文本提交直接进入会话流程；阶段命令（`run S1`）保留为快捷入口
- 侧栏：会话列表、工具清单、累计成本与 token

### 3.6 科研技能层（阶段二）

- **阶段技能化**：S1–S8 各自实现为 `StageAgent`，同时注册为内核工具；保留确定性编排模板 `research_pipeline`（S1→S8 固定顺序 + 回退边）
- **领域包**：`domain_packs/{pack}/manifest.yaml` 声明判定规则、检验选择表、报告规范；加载器按 `projects.domain` 装载
- **科研工具**：`search_literature`、`parse_pdf`、`verify_citation`、`select_stat_test`、`run_experiment`、`package_reproduction`
- **可复现性落点**：每次解析与执行写 `env_snapshot`（Python / 依赖 / OS / GPU）、`random_seed`、`code_hash`
- **Critic**：异构厂商模型 + 否决权，对抗评审闭环
- **回退边**：阶段产出不达阈值时回到上游阶段，落 `decisions` 与检查点

**两阶段衔接约定**

1. `run_pipeline` 注册为内核 capability，编排不只在 API 层可达
2. 任务计划以结构化对象落库，科研模式通过替换计划模板实现
3. 内核支持 `deterministic` 编排模式（固定顺序 + 固定种子）
4. 对话层不持有研究状态，研究状态始终在结构化黑板

---

## 4. 冲刺排期

| Sprint | 周次 | 日期 | 冲刺目标 | 关键交付 | 依赖 | 里程碑 | SP |
|:---:|:---:|---|---|---|---|:---:|---:|
| **S3** | W1–W2 | 09-21 ~ 10-04 | 技术债清偿 + 异步地基 + 自定义模型接入 | FIX-01~09 完成；`jobs`/`job_events` + SSE；纵向切片走查；用户自定义 Provider 接入（增删改 + 模型探测 + 设置页表单）；`docs/adr/` 与 CI 起步 | — | — | 56 |
| **S4** | W3–W4 | 10-05 ~ 10-18 | **Agent 内核** | 工具注册表 + 调用循环 + 权限分级与最小沙箱 + 结构化任务计划 + 会话与上下文管理 + 内核端到端冒烟 | S3 的 SSE 地基 | — | 48 |
| **S5** | W5–W6 | 10-19 ~ 11-01 | S2 前半：检索聚合 + 解析管线 | 多源检索（OpenAlex / Crossref / arXiv / S2）+ 限流 + 去重 + 排序；检索策略记录器；解析管线（特征判定 → 路由 → pdfplumber / LaTeX / OCR 兜底 → 质量评分）；S1 研究知识图谱与空白点扫描 | S4 内核 | — | 44 |
| **S6** | W7–W8 | 11-02 ~ 11-15 | S2 后半：检索增强 + 精读 + 溯源 | 分块 + Embedding + FTS5 + 混合检索 + RRF + rerank；引用坐标校验器（跳转原文高亮）；Librarian 迭代检索（证据饱和判定）；精读卡片；领域包机制接入 `projects.domain` + `domain_packs/cs-ai` | S5 | **M2 文献闭环** | 46 |
| **S7** | W9–W10 | 11-16 ~ 11-29 | S3 假设形式化 + 编排闭环 | Formalizer 可证伪性检查器 + 变量完备性；回退边 + 编排闭环；金标评测集构建（20 篇） | S6 | — | 44 |
| **S8** | W11–W12 | 11-30 ~ 12-13 | Critic 对抗评审 + S4 设计 | Critic 异构评审（否决权）+ 对抗评审闭环；风险分级人工检查点；Designer 样本量估算器 + 设计模式检查器 | S7 | **M3 编排闭环** | 46 |
| **S9** | W13–W14 | 12-14 ~ 12-27 | S5 判定 + 完整沙箱 + 真实执行 | Feasibility Gate（五道检查 + 逐项依据 + 用户覆盖）；代码执行沙箱（目录白名单 / 禁网 / 资源配额 / 产物哈希）；真实执行分支（代码生成 → 静态检查 → 执行 → 失败自愈 ≤3 → 多种子 ≥3）；Handover Pack；执行真实性标注 | S8 | **M4 执行闭环** | 50 |
| **S10** | W15–W16 | 12-28 ~ 01-10 | S6 分析解读 + 成本优化 | 统计检验选择规则引擎（确定性）+ 图表 + 结论一致性校验；成本与性能优化 | S9 | — | 50 |
| **S11** | W17–W18 | 01-11 ~ 01-24 | S7 写作 + S8 复现 + 交付 | IMRaD 结构化草稿 + CSL 格式化 + 术语一致性 + 引用真实性校验；复现包 + 可跑通校验器；端到端联调；反事实与消融评测；文档 / 开源规范 / 桌面打包 / 演示视频 / 答辩 | S10 | **M5 全链路闭环 · M6 交付** | 58 |

**说明**：S11 为交付冲刺，承载内容偏多，执行时按 §7 裁剪方案从上到下削减。

---

## 5. 冲刺待办

### 5.1 Sprint 3 待办（US-301 ~ US-312，56 SP）

| 编号 | 故事 | SP | 对齐 | 状态 |
|---|---|:--:|---|---|
| US-301 | 按 README 配好真实模型后正常启动并用它跑通 S1 | 3 | FIX-01 | 部分完成（启动路径已通，真实 Key 跑通并入 US-311） |
| US-302 | 批准预算超限后真正恢复运行，不再反复弹审批 | 5 | FIX-02 | 已完成 |
| US-303 | 未配 Key 时得到「请录入 Key」的提示 | 3 | FIX-04 | 已完成 |
| US-304 | 长任务执行时看到实时进度 | 15 | FIX-03 | 待办 |
| US-305 | 重启后熔断状态与缓存仍然有效 | 5 | FIX-05 | 待办 |
| US-306 | 流水线在暂停时停下来，不继续跑后面阶段 | 2 | FIX-07 | 已完成 |
| US-307 | 一眼看出哪些阶段已实现、哪些是占位 | 2 | FIX-08 | 待办 |
| US-308 | CI 在每次提交上跑测试与构建 | 3 | FIX-10 | 待办 |
| US-309 | ADR 记录与设计文档的偏离 | 2 | FIX-11 | 待办 |
| US-310 | 补齐三类缺口测试，避免同类问题再次漏出 | 8 | FIX-12 | 部分完成 |
| US-311 | 纵向切片走查：自由文本入口 → S1 → SSE 实时进度 → 结果落库 → 刷新仍在 → 成本与轨迹可见 | 2 | — | 待办 |
| US-312 | 用户自定义模型接入：Provider 新增 / 修改 / 删除 / 模型探测 API，设置页接入表单与连通性校验 | 6 | — | 待办 |

**US-312 验收标准**
用户在设置页填写 `name` / `type` / `base_url` / `models` / `vendor` / `api_key_ref` / `capabilities` / `price` 后即可用，无需改代码或重启；`POST /api/settings/providers/{name}/probe-models` 能拉取或提示手工填写；删除 provider 时若被档位路由或 Agent 引用则拒绝并列出引用位置；`PATCH /api/settings/providers/{name}` 修改 `base_url` 或 `capabilities` 后立即重建注册表并失效该 provider 的健康探测缓存。

### 5.2 Sprint 4 待办（US-401 ~ US-408，48 SP）

| 编号 | 故事 | SP |
|---|---|:--:|
| US-401 | 会话与消息持久化、多轮上下文加载 | 6 |
| US-402 | 上下文裁剪与 token 预算 | 4 |
| US-403 | 结构化任务计划：落库、可人工修改、支持编排模板与确定性模式 | 9 |
| US-404 | 工具注册表：JSON Schema / 权限等级 / 幂等性标注；`run_pipeline` 注册为 capability | 6 |
| US-405 | 工具调用循环：ReAct 与 Plan-Execute 双模式、并行调用、结果截断、错误回传自愈 ≤3 | 9 |
| US-406 | 工具权限分级与最小沙箱：目录白名单 / 禁网 / 危险操作人工批准 | 8 |
| US-407 | 执行控制：步数上限、预算熔断、中断与检查点恢复 | 3 |
| US-408 | 内核端到端冒烟 | 3 |

**Sprint 4 验收标准（Done 定义）**
用 3 个工具（`read_file` / `write_file` / `run_command`）完成一个 ≥5 步的真实任务；全程流式可见；执行命令前触发人工批准；预算超限能暂停并恢复；中断后能从检查点继续；`GET /api/tools` 返回全部工具及权限等级。

### 5.3 Sprint 5 ~ S11 待办

各冲刺按 §3.6 与 §4 的关键交付拆分故事，在冲刺启动时按 Sprint 3 / Sprint 4 的颗粒度补全待办表。

---

## 6. 决策闸口

| 闸口 | 时点 | 通过标准 | 未通过时的动作 |
|:---:|---|---|---|
| **G1** | S3 末（W2，10-04） | ① 配一次真实模型 Key 能跑通 S1；② 一个 30 秒以上的任务全程有实时进度；③ 预算批准后真能恢复；④ 纵向切片走查留证 | 不进入 S4。先补齐异步地基与纵向切片 |
| **G2** | S4 末（W4，10-18） | 内核验收：3 工具 × ≥5 步真实任务跑通、流式可见、危险操作可批准、中断可恢复、`deterministic` 模式可用 | 不进入 S5。内核压缩为「工具注册表 + 调用循环」，暂缓并行调用与上下文摘要 |
| **G3** | S6 末（W8，11-15） | S2 端到端可演示：输入关键词 → 30 篇去重文献 → 解析入库 → 问答 → 引用可点击溯源（可溯源率 ≥95%） | 压缩 S11 的 S7/S8 深度：S7 降级为「结构化草稿 + 引用」，砍术语一致性与 CSL 高级格式 |
| **G4** | S9 末（W14，12-27） | 计算机类实验能真跑出结果（沙箱 → 代码 → 指标 → 多种子）；湿实验方案能输出完备交接包 | S5 降级为「判定 + 方案交付」，真实执行列为延伸目标；腾出时间补 M5 全链路演示（元分析场景） |

**永不削减的四条底线**：引用可溯源 · 计算可复现 · 执行真实性标注 · Critic 异构评审。

---

## 7. 周期不足时的裁剪方案

按「从后往前砍」的顺序执行：

| 可用周期 | 方案 | 交付范围 |
|---|---|---|
| **18 周** | 按 §4 完整执行 | 完整 8 阶段，S5 条件执行 |
| **17 周** | S11 压缩：桌面安装包降级为免安装 zip；演示视频与答辩材料减半 | 同上，打包形态降级 |
| **16 周** | 合并 S10 与 S11 的部分内容；S8 复现包只做打包不做「干净环境校验」；评测缩为 20 篇金标 | 8 阶段，S8 手工复核 |
| **15 周** | S10 的成本与性能优化挪出范围；S6 图表降级为静态导出 | 8 阶段完整，无优化项 |
| **14 周** | 砍掉 S8 投稿复现阶段；S5 只保留计算类分支 | S1~S7 完整 |
| **12 周** | S4 内核压缩为「工具注册表 + 调用循环 + 权限闸门」，暂缓并行调用、上下文摘要、确定性模式 | 内核最小可用 |

**裁剪优先级（从低到高保留）**：
桌面打包 → 演示视频与答辩材料 → 术语一致性检查 → CSL 高级格式 → S8 期刊匹配 → 成本与性能优化 → 并行工具调用 → 上下文摘要化 → 内核确定性模式 → 四条底线。

---

## 8. 执行顺序

### 8.1 Sprint 3 内顺序（每步一个 commit，每步跑一次回归）

| # | 提交范围 | 内容 |
|:--:|---|---|
| 1 | `feat(US-307)` | 阶段实现标记与占位阶段置灰 |
| 2 | `docs(US-309)` | `docs/adr/` 目录与索引；ADR-0001 前端会话式单流界面、ADR-0002 技术栈版本、ADR-0003 异步作业与 SSE、ADR-0004 对话层与结构化黑板的职责边界 |
| 3 | `feat(US-308)` | `.github/workflows/ci.yml`：后端 ruff + pytest；前端 oxlint + tsc + build |
| 4 | `feat(US-305)` | migration 4（`app_config` / `llm_cache`）；熔断状态落库；缓存改 SQLite + TTL；缓存键改用实际命中 provider |
| 5 | `feat(US-312)` | Provider 新增 / 修改 / 删除 / 模型探测 API；设置页接入表单；删除前的路由引用校验 |
| 6 | `feat(US-304)` | migration 4（`jobs` / `job_events`）；`app/jobs/` 作业层；编排层事件接线 |
| 7 | `feat(US-304)` | `POST /run` 改受理接口；`GET /api/jobs/{id}`、`/stream` SSE |
| 8 | `feat(US-311)` | 自由文本入口改走 S1；真实 Key 走查；纵向切片留证 |
| 9 | `feat(US-304)` | 前端订阅作业流、实时渲染步骤、失败回退轮询 |
| 10 | `test(US-310)` | 补齐作业 / SSE / 持久化 / 纵向切片冒烟测试 |
| 11 | `docs(Sprint 3)` | README 更新；G1 闸口结论留证 |

### 8.2 Sprint 4 内顺序

| # | 提交范围 | 内容 |
|:--:|---|---|
| 1 | `feat(US-401/402)` | migration 5（`conversations` / `messages`）；会话 API；上下文装配与裁剪 |
| 2 | `feat(US-403)` | `task_plans` 表与计划器；计划 API；确定性模式开关 |
| 3 | `feat(US-404)` | 工具契约与注册表；`GET /api/tools`；`run_pipeline` 注册为 capability |
| 4 | `feat(US-405)` | 内核循环（双模式、并行调用、结果截断、错误自愈） |
| 5 | `feat(US-406)` | 权限分级；最小沙箱；危险操作批准接线 |
| 6 | `feat(US-407)` | 执行控制：步数上限、预算熔断、中断、检查点恢复 |
| 7 | `feat` | 前端会话界面：计划卡片、工具调用卡片、批准卡片、工具清单 |
| 8 | `test(US-408)` | 内核端到端冒烟与回归 |

### 8.3 Sprint 5 起

按 §4 关键交付逐冲刺拆解，沿用同颗粒度。

---

## 9. 技术债修复清单

| 编号 | 修复动作 | 验收 | 状态 |
|---|---|---|---|
| FIX-01 | `PROVIDER_TYPES` 注册 `openai_compat`；补「配置文件 → ProviderRegistry → Router」集成测试 | 配置含 `openai_compat` 时应用可启动；`GET /api/settings/providers` 可见该 provider | 已完成 |
| FIX-02 | 新增 `budget_grants` 表与迁移 `9f3c1a7b5d20`；有效限额 = 配置限额 + 未过期豁免；批准时签发豁免并写审计决策；新增 `GET /api/projects/{id}/budget-grants` | 预算触发 → 批准 → 重跑成功；不再产生新的 pending 审批；豁免可审计 | 已完成 |
| FIX-03 | 建 `jobs` / `job_events` 表；进程内单 worker 队列；`POST /run` 改受理接口返回 `job_id`；新增 `GET /api/jobs/{id}` 与 `/stream` SSE（支持 `Last-Event-ID`）；前端订阅 + 轮询回退 | 受理接口 P95 < 100ms；30 秒以上任务全程有进度；断线重连不丢事件 | 待办 |
| FIX-04 | `health()` 改三态 `ok` / `unconfigured` / `down`；注册表不按健康剔除；`check_available()` 在调用点判定并给可操作提示；探测加 30s TTL 缓存；录入 Key 后主动失效 | 先改配置后录 Key 也能启动；未录 Key 运行阶段得到「请录入 Key」提示 | 已完成 |
| FIX-05 | 熔断状态落 `app_config`，`opened_at` 改 wall-clock；`reload()` 保留未变更 provider 的熔断计数；缓存改 `llm_cache` 表 + TTL（默认 7 天）；缓存键改用实际命中的 provider / model | 重启后熔断状态保留；缓存跨重启命中；降级恢复后不再返回旧缓存 | 待办 |
| FIX-06 | 新增 `app/ai/json_utils.py::extract_json()`：剥离代码围栏 → 平衡括号扫描 → 尾随逗号与单引号修复；`degrade` 与各 Agent 统一走它；解析失败将原始输出落 `agent_steps` | 带围栏 / 带前后缀 / 带尾逗号的输出均可解析 | 已完成 |
| FIX-07 | `run_pipeline` 遇 `paused` 立即中断并把后续阶段标 `skipped`，写决策日志；`approvals_dao.decide` 补写 `decided_at` | 一次预算熔断只产生 1 条审批 | 已完成 |
| FIX-08 | `StageAgent` 增 `implemented` 与 `planned_sprint`；`StageOut` 下发；前端未实现阶段置灰并给 tooltip | 界面上能一眼区分已实现与占位阶段 | 待办 |
| FIX-09 | `reload()` 返回类型改为明确结构；`_validate_vendor_separation` 覆盖整条候选链；补 `per_call` 单次调用上限；`health_report()` 复用探测缓存 | 类型标注与实返一致；厂商分离校验覆盖整条链 | 待办 |
| FIX-10 | `.github/workflows/ci.yml`：后端 ruff + pytest；前端 oxlint + tsc + build | 工作流在 push 与 pull_request 上运行 | 待办 |
| FIX-11 | 建 `docs/adr/`：模板、索引、ADR-0001 会话式单流界面、ADR-0002 技术栈版本、ADR-0003 异步作业与 SSE、ADR-0004 对话层职责边界 | ADR 目录可读且与代码一致 | 待办 |
| FIX-12 | 补三类测试：配置文件 → 注册表 → 路由集成；Provider 契约负路径（401 / 429 / 超时 / 畸形 JSON / 围栏输出）；编排状态机（暂停 → 批准 → 恢复不循环、pipeline 中断、回退边）。Sprint 4 追加内核冒烟 | 测试数与覆盖缺口逐项对齐 | 部分完成 |
